# 通用模块使用指南

## 📚 文档索引

本指南包含以下文档：

1. **技能文档**: `.trae/skills/通用模块调用指南/SKILL.md`
2. **规则文档**: `.trae/rules/rule.md` (已更新)
3. **项目文档**: 各项目的 `通用模块使用说明.md`

---

## 🎯 通用模块概述

### 什么是通用模块

通用模块是指在多个项目中**重复出现**、**功能相似**、**可以统一实现**的代码模块。

### 当前项目的通用模块

| 模块 | 服务地址 | 功能 | 支持项目 |
|------|----------|------|----------|
| 通知服务 | `http://43.156.242.184:8766/api/v1` | 统一发送飞书通知 | 5 个业务系统 |
| K 线服务 | `http://43.156.242.184:8765/api/v1` | 统一获取 K 线数据 | 需要行情数据的系统 |
| 数据库 | PostgreSQL | 统一的数据库连接 | 所有需要持久化的系统 |
| Redis | Redis | 统一的缓存和队列 | 所有需要缓存的系统 |

---

## 📋 快速开始

### 1. 飞书通知调用

```python
import requests

NOTIFICATION_URL = "http://43.156.242.184:8766/api/v1/send"

def send_notification(project: str, message: str, level: str = "info"):
    """
    发送通知到通用服务
    
    Args:
        project: 项目标识 (btc_eth, new_coin, grid, stock, inspection)
        message: 消息内容
        level: 通知级别 (info, warning, error)
    """
    response = requests.post(
        f"{NOTIFICATION_URL}/send",
        json={
            "project": project,
            "message": message,
            "type": "text",
            "level": level
        },
        timeout=10
    )
    return response.json()

# 使用示例
result = send_notification("btc_eth", "BTC 突破 70000 美元！", level="warning")
print(f"通知发送结果：{result}")
```

### 2. K 线数据调用

```python
import requests

KLINE_SERVICE_URL = "http://43.156.242.184:8765/api/v1"

def get_klines(symbol: str, interval: str, limit: int = 100):
    """
    获取 K 线数据
    
    Args:
        symbol: 交易对 (BTCUSDT, ETHUSDT)
        interval: K 线周期 (1m, 5m, 1h, 1d)
        limit: 数量限制
    
    Returns:
        K 线数据列表
    """
    response = requests.get(
        f"{KLINE_SERVICE_URL}/klines",
        params={
            "symbol": symbol,
            "interval": interval,
            "limit": limit
        },
        timeout=10
    )
    result = response.json()
    if result.get('code') == 0:
        return result['data']['klines']
    else:
        raise Exception(result.get('message'))

# 使用示例
klines = get_klines("BTCUSDT", "1h", limit=100)
for kline in klines:
    print(f"时间：{kline[0]}, 开盘：{kline[1]}, 收盘：{kline[4]}")
```

---

## 📖 详细文档

### 技能文档

**位置**: `.trae/skills/通用模块调用指南/SKILL.md`

**内容**:
- 如何识别通用模块
- 如何提炼通用模块
- 如何调用通用模块
- 实战案例
- 最佳实践

### 规则文档

**位置**: `.trae/rules/rule.md`

**内容**:
- 通用模块使用规范
- 通用模块识别标准
- 通用模块提炼流程
- 飞书通知调用规范
- K 线数据调用规范

### 项目文档

每个项目都在 `docs` 目录下有 `通用模块使用说明.md` 文档：

1. **BTC/ETH 交易系统**: `bianace_btcethbnb_trade/docs/通用模块使用说明.md`
2. **新币做空系统**: `bianace_newtrade_trade/docs/通用模块使用说明.md`
3. **网格交易系统**: `Grid_Trading/docs/通用模块使用说明.md`
4. **股票筛选系统**: `stockfilter/docs/通用模块使用说明.md`
5. **检查自动化系统**: `inspection_automation/docs/通用模块使用说明.md`

**内容**:
- 已提取的通用模块
- 调用方式和示例代码
- 配置说明和环境变量
- 备份文件和回滚方案

---

## 🔍 如何识别通用模块

### 识别标准

#### 1. 重复性检查
如果 3 个以上项目有相似代码 → 应该提炼为通用模块

```bash
# 搜索飞书通知代码
grep -r "webhook" /Users/yl/vscode/*/

# 搜索 K 线获取代码
grep -r "kline" /Users/yl/vscode/*/

# 搜索数据库连接代码
grep -r "postgresql" /Users/yl/vscode/*/
```

#### 2. 功能相似性检查

**通知功能示例**：
- 项目 A：直接调用飞书 webhook
- 项目 B：直接调用飞书 webhook
- 项目 C：直接调用飞书 webhook

**结论**：3 个项目都在做同样的事 → 应该提炼为通用通知服务

#### 3. 配置重复性检查

```bash
# 检查环境变量
grep "LARK_WEBHOOK" /Users/yl/vscode/*/.env

# 检查 API Key
grep "BINANCE_API_KEY" /Users/yl/vscode/*/.env
```

**判断标准**：
- ✅ 如果多个项目配置相同的变量 → 应该集中管理
- ⚠️ 如果配置相似但不同 → 考虑参数化

---

## 🏗️ 如何提炼通用模块

### 提炼流程

#### 第一步：需求分析
- 确定模块职责
- 设计接口规范
- 确定配置需求

#### 第二步：架构设计
- 设计服务结构
- 设计数据流
- 设计错误处理

#### 第三步：代码实现
- 遵循单一职责
- 接口清晰明确
- 完善错误处理
- 添加日志记录

#### 第四步：测试验证
- 单元测试
- 集成测试
- 压力测试
- 兼容性测试

#### 第五步：部署上线
- 构建 Docker 镜像
- 配置环境变量
- 启动容器
- 健康检查
- 监控日志

---

## 📊 项目标识说明

### 通知服务项目标识

| 项目 | 标识 | Webhook |
|------|------|---------|
| BTC/ETH 交易系统 | `btc_eth` | ✅ 已配置 |
| 新币做空系统 | `new_coin` | ✅ 已配置 |
| 网格交易系统 | `grid` | ✅ 已配置 |
| 股票筛选系统 | `stock` | ✅ 已配置 |
| 检查自动化系统 | `inspection` | ✅ 已配置 |

### 使用示例

```python
# BTC/ETH 交易系统
send_notification("btc_eth", "分析任务完成", level="info")

# 新币做空系统
send_notification("new_coin", "发现做空信号", level="warning")

# 网格交易系统
send_notification("grid", "网格创建成功", level="info")

# 股票筛选系统
send_notification("stock", "筛选出 3 只股票", level="info")

# 检查自动化系统
send_notification("inspection", "服务器巡检完成", level="info")
```

---

## 🎯 最佳实践

### 1. 命名规范

**项目标识命名**：
- ✅ `btc_eth` - 使用下划线分隔
- ✅ `new_coin` - 简洁明了
- ❌ `BTCETH` - 不要全大写
- ❌ `btc-eth` - 不要使用中划线

**通知级别**：
- `info` - 普通通知
- `warning` - 警告通知
- `error` - 错误通知

### 2. 错误处理

```python
try:
    result = send_notification(...)
    if not result:
        logger.error("通知发送失败")
except Exception as e:
    logger.error(f"通知异常：{e}")
    # 降级处理：记录到本地日志
```

### 3. 性能优化

```python
# 使用连接池
import requests
from requests.adapters import HTTPAdapter

session = requests.Session()
session.mount('http://', HTTPAdapter(pool_connections=10, pool_maxsize=20))

# 复用 session 发送通知
def send_notification(message: str):
    response = session.post(url, json=data, timeout=10)
    return response.json()
```

### 4. 监控告警

```python
# 添加重试机制
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=1, max=10))
def send_notification(message: str):
    response = requests.post(url, json=data, timeout=10)
    response.raise_for_status()
    return response.json()
```

---

## 🔗 相关资源

### 文档
- 技能文档：`.trae/skills/通用模块调用指南/SKILL.md`
- 规则文档：`.trae/rules/rule.md`
- 改造报告：`common_service/docs/reports/业务系统通知服务改造完成总结.md`

### 服务
- 通知服务：`http://43.156.242.184:8766/api/v1`
- K 线服务：`http://43.156.242.184:8765/api/v1`
- 健康检查：`curl http://43.156.242.184:8766/api/v1/health`

### 日志
```bash
# 查看通知服务日志
docker logs --tail 100 common_service_notification

# 过滤特定项目日志
docker logs --tail 100 common_service_notification | grep btc_eth
```

---

**文档版本**: v1.0  
**最后更新**: 2026-04-20  
**维护者**: AI Assistant
