# 通知服务测试报告

## 测试时间
2026-04-20

## 测试环境
- 服务器：43.156.242.184
- 服务：common_service_notification
- 端口：8766 (外部) -> 8000 (容器内部)

## 测试结果

### ✅ 全部测试通过

1. **容器内测试** - 成功
   ```bash
   docker exec common_service_notification python /tmp/test_container.py
   # 状态码：200
   # 响应：{"code":0,"message":"Message queued",...}
   ```

2. **服务器本地测试** - 成功
   ```bash
   curl -X POST 'http://localhost:8766/api/v1/send' \
     -H 'Content-Type: application/json' \
     -d '{"project":"btc_eth","message":"测试","type":"text"}'
   # 返回：{"code":0,"message":"Message queued",...}
   ```

3. **健康检查** - 成功
   ```bash
   curl http://localhost:8766/api/v1/health
   # {"code":0,"message":"ok","data":{"status":"healthy",...}}
   ```

4. **飞书发送** - 成功
   - 日志显示：`飞书消息发送成功：btc_eth`
   - 5 个项目 webhook 全部配置

### ✅ 外部访问测试 - 成功

1. **本地机器访问服务器** - 成功
   - 从本地访问 `http://43.156.242.184:8766` 正常
   - 所有 5 个项目测试全部通过
   - 飞书消息发送成功

2. **安全组配置**
   - 已配置腾讯云安全组规则
   - 入站规则：TCP:8766，来源：全部 IPv4 地址，策略：允许

## 安全组配置（已完成）

已在腾讯云控制台配置安全组：

1. 登录腾讯云控制台
2. 进入 安全组 页面
3. 找到实例 43.156.242.184 使用的安全组
4. 添加入站规则：
   - 协议端口：TCP:8766
   - 来源：全部 IPv4 地址
   - 策略：允许

## 验证命令

### 在服务器上验证
```bash
# 测试健康检查
curl http://localhost:8766/api/v1/health

# 测试发送通知
curl -X POST 'http://localhost:8766/api/v1/send' \
  -H 'Content-Type: application/json' \
  -d '{"project":"btc_eth","message":"测试通知","type":"text","level":"info"}'

# 查看日志
docker logs --tail 20 common_service_notification
```

### 在本地验证（配置安全组后）
```bash
# 测试健康检查
curl http://43.156.242.184:8766/api/v1/health

# 测试发送通知
curl -X POST 'http://43.156.242.184:8766/api/v1/send' \
  -H 'Content-Type: application/json' \
  -d '{"project":"btc_eth","message":"测试通知","type":"text"}'
```

## 服务状态

- 容器状态：运行中 (healthy)
- 端口映射：8766 -> 8000
- Redis 连接：正常
- Worker 数量：3
- Webhook 配置：5 个项目全部配置

## 下一步

1. 配置腾讯云安全组开放 8766 端口
2. 验证外部访问
3. 进行业务系统改造
