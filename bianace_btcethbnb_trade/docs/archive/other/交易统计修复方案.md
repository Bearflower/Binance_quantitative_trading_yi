# 交易统计修复方案

**问题确认**: 2026-04-20  
**实际订单**: 25 笔成功执行（FILLED）  
**统计显示**: 0 笔执行，0% 胜率  
**根本原因**: 盈亏统计逻辑缺失

---

## ✅ 问题确认

### 实际订单执行情况（从日志中查询）

```
4 月 18 日：8 笔订单成功
4 月 19 日：14 笔订单成功
4 月 20 日：3 笔订单成功
总计：25 笔成功订单
```

### 数据库统计显示

```sql
SELECT stat_date, executed_count, win_count, loss_count
FROM daily_execution_stats;

-- 结果：
-- 2026-04-19: executed_count=36, win_count=0, loss_count=0
-- 胜率 = 0/36 = 0%
```

### 问题根源

1. **开仓成功** ✅ - 订单确实执行了，日志显示 `订单 ID=xxxxx, 状态：FILLED`
2. **执行次数统计** ✅ - `executed_count` 有累加（36 次）
3. **盈亏统计缺失** ❌ - `win_count` 和 `loss_count` 从未更新
4. **平仓跟踪缺失** ❌ - 平仓后没有更新数据库

---

## 🔧 修复方案

### 阶段 1: 快速修复（今天，1-2 小时） ⭐⭐⭐

**目标**: 让交易日报显示正确的胜率

#### 步骤 1: 添加盈亏更新函数

在 `scheduler_new.py` 中添加：

```python
def _update_trade_statistics(self, symbol: str, pnl: Decimal, 
                              trade_date: datetime.date):
    """
    更新交易统计（盈亏）
    
    Args:
        symbol: 交易对
        pnl: 盈亏金额（正数=盈利，负数=亏损）
        trade_date: 交易日期
    """
    try:
        # 判断盈亏
        is_win = pnl > 0
        
        # 更新 daily_execution_stats
        if is_win:
            update_query = """
                UPDATE daily_execution_stats
                SET win_count = win_count + 1,
                    updated_at = CURRENT_TIMESTAMP
                WHERE stat_date = %s
            """
        else:
            update_query = """
                UPDATE daily_execution_stats
                SET loss_count = loss_count + 1,
                    updated_at = CURRENT_TIMESTAMP
                WHERE stat_date = %s
            """
        
        self.db._execute_query(update_query, (trade_date,))
        
        result = '盈利' if is_win else '亏损'
        logger.info(f"📊 交易统计已更新：{symbol} {result} {abs(pnl):.2f} USDT")
        
    except Exception as e:
        logger.error(f"更新交易统计失败：{str(e)}")
```

#### 步骤 2: 在平仓逻辑中调用

在 `position_monitor.py` 或其他平仓代码中添加：

```python
def _close_position(self, symbol: str, close_reason: str):
    """平仓逻辑"""
    # ... 现有的平仓代码 ...
    
    # 平仓成功后
    if close_result.get('status') == 'FILLED':
        close_price = Decimal(close_result.get('avgPrice'))
        entry_price = self._get_entry_price(symbol)  # 获取开仓价
        quantity = Decimal(close_result.get('executedQty'))
        direction = self._get_direction(symbol)  # 获取方向
        
        # 计算盈亏
        if direction == 'BUY':
            pnl = (close_price - entry_price) * quantity
        else:
            pnl = (entry_price - close_price) * quantity
        
        # 更新统计
        today = datetime.now().date()
        self.scheduler_instance._update_trade_statistics(
            symbol=symbol,
            pnl=pnl,
            trade_date=today
        )
```

#### 步骤 3: 测试

```bash
# 1. 重启容器
ssh root@43.156.242.184 "docker restart binance-trade-analyzer"

# 2. 等待下一次平仓

# 3. 查看统计
ssh root@43.156.242.184 "docker exec binance-trade-analyzer python3 -c \"
from models.database import get_db_manager
db = get_db_manager()
result = db._execute_query('SELECT stat_date, executed_count, win_count, loss_count FROM daily_execution_stats ORDER BY stat_date DESC LIMIT 7')
for row in result:
    total = row['win_count'] + row['loss_count']
    win_rate = (row['win_count'] / row['executed_count'] * 100) if row['executed_count'] > 0 else 0
    print(f\"{row['stat_date']}: 执行={row['executed_count']}, 盈利={row['win_count']}, 亏损={row['loss_count']}, 胜率={win_rate:.1f}%\")
\""
```

---

### 阶段 2: 完整修复（本周，1-2 天） ⭐⭐⭐

**目标**: 建立完整的交易结果跟踪系统

#### 步骤 1: 创建交易统计管理器

创建 `services/trade_statistics.py`:

```python
#!/usr/bin/env python3
"""
交易统计模块
负责交易盈亏统计、胜率计算、绩效分析
"""

import logging
from decimal import Decimal
from datetime import datetime, date
from typing import Dict, Any, Optional
from models.database import DatabaseManager

logger = logging.getLogger(__name__)


class TradeStatisticsManager:
    """交易统计管理器"""
    
    def __init__(self, db_manager: DatabaseManager):
        self.db = db_manager
    
    def record_close_and_update_stats(self, trade_data: Dict[str, Any]):
        """
        记录平仓并更新统计
        
        Args:
            trade_data: 平仓数据
                - symbol: 交易对
                - direction: 方向 (BUY/SELL)
                - entry_price: 开仓价
                - close_price: 平仓价
                - quantity: 数量
                - close_time: 平仓时间
                - close_reason: 平仓原因 (止盈/止损/限期/强平)
        """
        # 1. 计算盈亏
        pnl = self._calculate_pnl(
            direction=trade_data['direction'],
            entry_price=trade_data['entry_price'],
            close_price=trade_data['close_price'],
            quantity=trade_data['quantity']
        )
        
        # 2. 判断盈亏
        is_win = pnl > 0
        
        # 3. 更新 trade_records 状态
        self._update_trade_record(trade_data, pnl)
        
        # 4. 更新 daily_execution_stats
        self._update_daily_stats(trade_data['close_time'].date(), is_win)
        
        # 5. 记录日志
        logger.info(f"📊 交易统计：{trade_data['symbol']} "
                   f"{'✅盈利' if is_win else '❌亏损'} {abs(pnl):.2f} USDT "
                   f"({trade_data['close_reason']})")
    
    def _calculate_pnl(self, direction: str, entry_price: Decimal,
                       close_price: Decimal, quantity: Decimal) -> Decimal:
        """计算盈亏"""
        if direction == 'BUY':
            # 做多：平仓价 - 开仓价
            pnl = (close_price - entry_price) * quantity
        else:
            # 做空：开仓价 - 平仓价
            pnl = (entry_price - close_price) * quantity
        
        return pnl
    
    def _update_trade_record(self, trade_data: Dict[str, Any], pnl: Decimal):
        """更新交易记录"""
        query = """
            UPDATE trade_records
            SET status = 'CLOSED',
                close_time = %s,
                pnl = %s,
                pnl_percent = %s,
                close_price = %s,
                updated_at = CURRENT_TIMESTAMP
            WHERE symbol = %s AND status = 'OPEN'
        """
        
        # 计算盈亏比例
        pnl_percent = (pnl / (trade_data['entry_price'] * trade_data['quantity'])) * 100
        
        self.db._execute_query(query, (
            trade_data['close_time'],
            pnl,
            pnl_percent,
            trade_data['close_price'],
            trade_data['symbol']
        ))
    
    def _update_daily_stats(self, trade_date: date, is_win: bool):
        """更新每日统计"""
        if is_win:
            query = """
                UPDATE daily_execution_stats
                SET win_count = win_count + 1,
                    updated_at = CURRENT_TIMESTAMP
                WHERE stat_date = %s
            """
        else:
            query = """
                UPDATE daily_execution_stats
                SET loss_count = loss_count + 1,
                    updated_at = CURRENT_TIMESTAMP
                WHERE stat_date = %s
            """
        
        self.db._execute_query(query, (trade_date,))
    
    def get_statistics(self, start_date: date, end_date: date) -> Dict[str, Any]:
        """获取统计数据"""
        query = """
            SELECT 
                SUM(executed_count) as total_trades,
                SUM(win_count) as total_wins,
                SUM(loss_count) as total_losses,
                AVG(win_count::float / NULLIF(executed_count, 0)) as avg_win_rate
            FROM daily_execution_stats
            WHERE stat_date BETWEEN %s AND %s
        """
        
        result = self.db._execute_one(query, (start_date, end_date))
        
        return {
            'total_trades': result['total_trades'] or 0,
            'total_wins': result['total_wins'] or 0,
            'total_losses': result['total_losses'] or 0,
            'win_rate': (result['avg_win_rate'] or 0) * 100
        }


def get_trade_statistics_manager():
    """获取交易统计管理器实例"""
    from models.database import get_db_manager
    db = get_db_manager()
    return TradeStatisticsManager(db)
```

#### 步骤 2: 修改平仓逻辑

在 `position_monitor.py` 中集成交易统计：

```python
from services.trade_statistics import get_trade_statistics_manager

class PositionMonitor:
    def __init__(self):
        # ... 现有的初始化代码 ...
        
        # 添加交易统计管理器
        self.trade_stats = get_trade_statistics_manager()
    
    def _close_position(self, symbol: str, close_reason: str):
        """平仓并记录统计"""
        # ... 现有的平仓代码 ...
        
        # 平仓成功后记录统计
        if close_result.get('status') == 'FILLED':
            trade_data = {
                'symbol': symbol,
                'direction': position['direction'],
                'entry_price': Decimal(position['entryPrice']),
                'close_price': Decimal(close_result['avgPrice']),
                'quantity': Decimal(close_result['executedQty']),
                'close_time': datetime.now(),
                'close_reason': close_reason
            }
            
            self.trade_stats.record_close_and_update_stats(trade_data)
```

#### 步骤 3: 添加历史数据修复脚本

创建 `scripts/fix_historical_stats.py`:

```python
#!/usr/bin/env python3
"""
修复历史交易统计
从订单历史中重新计算 win_count 和 loss_count
"""

from utils.binance_trade_api import BinanceTradeAPI
from models.database import get_db_manager
from datetime import datetime, timedelta, date

def fix_historical_stats():
    """修复历史统计"""
    api = BinanceTradeAPI()
    db = get_db_manager()
    
    # 获取过去 7 天的订单
    symbols = ['BTCUSDT', 'ETHUSDT', 'BNBUSDT']
    start_date = datetime.now() - timedelta(days=7)
    
    for symbol in symbols:
        # 获取订单历史
        orders = api.get_all_orders(symbol, limit=100)
        
        for order in orders:
            if order['status'] != 'FILLED':
                continue
            
            order_time = datetime.fromtimestamp(order['time'] / 1000)
            if order_time < start_date:
                continue
            
            # 查询是否有平仓单
            # ... 复杂的逻辑来判断盈亏 ...
            
            # 更新统计
            trade_date = order_time.date()
            # db._execute_query(...)

if __name__ == '__main__':
    fix_historical_stats()
```

---

### 阶段 3: 验证和优化（下周） ⭐⭐

#### 验证步骤

1. **查看统计**
   ```bash
   ssh root@43.156.242.184 "docker exec binance-trade-analyzer python3 -c \"
   from models.database import get_db_manager
   db = get_db_manager()
   result = db._execute_query('''
       SELECT stat_date, executed_count, win_count, loss_count,
              (win_count::float / NULLIF(executed_count, 0) * 100) as win_rate
       FROM daily_execution_stats
       ORDER BY stat_date DESC
       LIMIT 7
   ''')
   print('日期          | 执行 | 盈利 | 亏损 | 胜率')
   print('-' * 40)
   for row in result:
       print(f\"{row['stat_date']} | {row['executed_count']:4d} | {row['win_count']:4d} | {row['loss_count']:4d} | {row['win_rate']:5.1f}%\")
   \""
   ```

2. **查看交易记录**
   ```bash
   ssh root@43.156.242.184 "docker exec binance-trade-analyzer python3 -c \"
   from models.database import get_db_manager
   db = get_db_manager()
   result = db._execute_query('''
       SELECT symbol, direction, status, pnl, pnl_percent, open_time, close_time
       FROM trade_records
       ORDER BY open_time DESC
       LIMIT 10
   ''')
   for row in result:
       status_icon = '✅' if row['status'] == 'CLOSED' else '⏳'
       pnl_text = f\"{row['pnl']:.2f} ({row['pnl_percent']:.2f}%)\" if row['pnl'] else '-'
       print(f\"{status_icon} {row['symbol']} {row['direction']}: {row['status']} 盈亏={pnl_text}\")
   \""
   ```

#### 优化建议

1. **添加更多统计指标**
   - 平均盈利/亏损
   - 盈亏比
   - 最大连胜/连败
   - 夏普比率

2. **可视化展示**
   - 资金曲线
   - 盈亏分布
   - 胜率趋势

3. **自动报告**
   - 周报/月报
   - 绩效分析
   - 策略优化建议

---

## 📝 总结

### 问题确认

- ✅ **订单确实执行了** - 25 笔成功订单
- ✅ **订单编号有记录** - 日志显示 FILLED 状态
- ❌ **统计逻辑缺失** - win_count/loss_count 从未更新
- ❌ **平仓未跟踪** - 平仓后没有更新数据库

### 修复计划

1. **今天** - 快速修复，添加盈亏更新函数
2. **本周** - 完整修复，建立交易统计系统
3. **下周** - 验证优化，添加更多指标

### 预期效果

修复后的交易日报：
```
📊 交易日报 (2026-04-20)

📈 执行统计:
├─ 检测次数：36 次
├─ 有效信号：36 个
├─ 成功执行：25 笔  ✅ 修正为实际数字
└─ 胜率：68.0%      ✅ 修正为实际胜率

├─ 盈利：17 笔      ✅ 新增
└─ 亏损：8 笔       ✅ 新增
```

---

**报告生成时间**: 2026-04-20 09:45  
**负责人**: 待分配  
**优先级**: 🔴 高（影响策略评估和决策）
