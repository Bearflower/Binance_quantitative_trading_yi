# 股票形态筛选系统重构实施计划

**文档版本**: V1.0
**创建日期**: 2026-04-27
**最后更新**: 2026-04-27

---

## 1. 重构概览

### 1.1 重构范围

本次重构涉及以下内容:

| 重构项 | 当前状态 | 目标状态 | 工作量 |
|--------|----------|----------|--------|
| 回测器类文件 | 7个文件 | 1个文件 | 高 |
| 回测脚本文件 | 19个文件 | 3个文件 | 高 |
| 配置文件 | 3个文件 | 1个文件 | 中 |
| 文档文件 | 混乱 | 结构化 | 中 |
| 分析脚本 | 11个文件 | 整合到模块 | 低 |
| 项目结构 | 扁平化 | 模块化 | 中 |

### 1.2 重构原则

1. **渐进式重构**: 分阶段进行,每个阶段都可独立验证
2. **保持兼容**: 不破坏现有功能和接口
3. **充分测试**: 每个阶段都要进行充分测试
4. **文档同步**: 代码变更的同时更新文档

---

## 2. 详细实施步骤

### 2.1 阶段1: 准备工作(第1天)

#### 2.1.1 创建归档目录结构

**任务描述**: 创建用于存放历史版本代码和文档的目录结构。

**执行步骤**:

```bash
# 1. 创建归档根目录
mkdir -p archived

# 2. 创建归档子目录
mkdir -p archived/backtesters
mkdir -p archived/configs
mkdir -p archived/docs
mkdir -p archived/scripts

# 3. 创建归档说明文件
cat > archived/README.md << 'EOF'
# 归档文件说明

本目录存放项目历史版本的代码和文档,仅供参考,不建议修改。

## 目录结构

- `backtesters/`: 历史版本的回测器代码
- `configs/`: 历史版本的配置文件
- `docs/`: 历史版本的文档
- `scripts/`: 历史版本的脚本

## 版本说明

- V2.1: 2026-04-07发布
- V2.2: 2026-04-08发布
- V2.3: 2026-04-09发布
- V2.4: 2026-04-14发布(当前版本)
- V2.5: 对比测试版本

## 注意事项

1. 归档文件仅供参考,不建议修改
2. 如需使用历史版本,请联系项目负责人
3. 归档文件不参与常规测试和部署
EOF
```

**验收标准**:
- [ ] 归档目录创建完成
- [ ] 归档说明文件创建完成

#### 2.1.2 备份当前代码

**任务描述**: 备份当前代码,确保可以回滚。

**执行步骤**:

```bash
# 1. 创建备份目录
mkdir -p backups/$(date +%Y%m%d)

# 2. 备份当前代码
tar -czf backups/$(date +%Y%m%d)/stockfilter_backup.tar.gz \
  --exclude='data/backtest/baostocks' \
  --exclude='*.pyc' \
  --exclude='__pycache__' \
  --exclude='.git' \
  .

# 3. 记录备份信息
cat > backups/$(date +%Y%m%d)/backup_info.txt << EOF
备份时间: $(date)
备份原因: 项目重构前备份
备份文件: stockfilter_backup.tar.gz
备份大小: $(du -h backups/$(date +%Y%m%d)/stockfilter_backup.tar.gz | cut -f1)
EOF
```

**验收标准**:
- [ ] 备份文件创建完成
- [ ] 备份信息记录完成

#### 2.1.3 创建重构分支

**任务描述**: 创建Git分支用于重构工作。

**执行步骤**:

```bash
# 1. 创建重构分支
git checkout -b refactor/cleanup-project-structure

# 2. 推送分支到远程
git push -u origin refactor/cleanup-project-structure
```

**验收标准**:
- [ ] 重构分支创建完成
- [ ] 分支推送到远程完成

---

### 2.2 阶段2: 代码重构(第2-4天)

#### 2.2.1 创建统一回测器类(第2天)

**任务描述**: 创建统一的回测器类,支持版本切换。

**执行步骤**:

**步骤1: 分析现有回测器差异**

```bash
# 创建对比文档
cat > docs/references/backtester_versions_comparison.md << 'EOF'
# 回测器版本对比

## V2.2 回测器
- 跌幅阈值: 12%
- 放量涨幅: 5%
- 量比范围: 1.5-12倍
- 时间窗口: 10天

## V2.3 回测器
- 跌幅阈值: 10%
- 放量涨幅: 4%
- 量比范围: 1.3-13倍
- 时间窗口: 20天

## V2.4 回测器(当前版本)
- 跌幅阈值: 8%
- 放量涨幅: 3%
- 量比范围: 1.2-15倍
- 时间窗口: 60天

## V2.5 回测器
- 跌幅阈值: 9%
- 放量涨幅: 3.5%
- 量比范围: 1.3-15倍
- 时间窗口: 50天
EOF
```

**步骤2: 创建统一回测器类**

创建文件 `core/backtester.py`:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
统一回测器:
- 支持多版本参数配置
- 通过配置文件切换版本
- 保留所有版本的核心功能
"""

import pandas as pd
import yaml
from typing import Optional, Dict, List
from datetime import datetime


class Backtester:
    """统一回测器,支持版本切换"""

    # 版本参数预设
    VERSION_PARAMS = {
        'v22': {
            'drop_threshold': 0.12,
            'surge_price_ratio': 0.05,
            'min_volume_ratio': 1.5,
            'max_volume_ratio': 12.0,
            'volume_shrink_ratio': 0.6,
            'shrink_to_surge_days': 10,
        },
        'v23': {
            'drop_threshold': 0.10,
            'surge_price_ratio': 0.04,
            'min_volume_ratio': 1.3,
            'max_volume_ratio': 13.0,
            'volume_shrink_ratio': 0.7,
            'shrink_to_surge_days': 20,
        },
        'v24': {
            'drop_threshold': 0.08,
            'surge_price_ratio': 0.03,
            'min_volume_ratio': 1.2,
            'max_volume_ratio': 15.0,
            'volume_shrink_ratio': 0.8,
            'shrink_to_surge_days': 60,
        },
        'v25': {
            'drop_threshold': 0.09,
            'surge_price_ratio': 0.035,
            'min_volume_ratio': 1.3,
            'max_volume_ratio': 15.0,
            'volume_shrink_ratio': 0.8,
            'shrink_to_surge_days': 50,
        },
    }

    def __init__(self, config_path: str = 'config/config.yaml', version: str = 'v24'):
        """
        初始化回测器

        Args:
            config_path: 配置文件路径
            version: 版本号(v22, v23, v24, v25)
        """
        # 加载配置文件
        with open(config_path, 'r', encoding='utf-8') as f:
            self.config = yaml.safe_load(f)

        # 设置版本
        self.version = version

        # 加载版本参数
        self._load_version_params()

        # 加载交易参数
        self._load_trading_params()

    def _load_version_params(self):
        """加载版本参数"""
        # 优先使用配置文件中的参数,其次使用预设参数
        version_config = self.config.get('versions', {}).get(self.version, {})

        # 获取版本预设参数
        default_params = self.VERSION_PARAMS.get(self.version, self.VERSION_PARAMS['v24'])

        # 合并参数(配置文件优先)
        self.drop_threshold = version_config.get('drop_threshold', default_params['drop_threshold'])
        self.surge_price_ratio = version_config.get('surge_price_ratio', default_params['surge_price_ratio'])
        self.min_volume_ratio = version_config.get('min_volume_ratio', default_params['min_volume_ratio'])
        self.max_volume_ratio = version_config.get('max_volume_ratio', default_params['max_volume_ratio'])
        self.volume_shrink_ratio = version_config.get('volume_shrink_ratio', default_params['volume_shrink_ratio'])
        self.shrink_to_surge_days = version_config.get('shrink_to_surge_days', default_params['shrink_to_surge_days'])

        # 方案B参数
        pattern_config = self.config.get('pattern', {})
        self.flat_days = pattern_config.get('flat_days', 0)
        self.post_surge_check_days = pattern_config.get('post_surge_check_days', 5)
        self.post_surge_max_drop = pattern_config.get('post_surge_max_drop', 0.97)

    def _load_trading_params(self):
        """加载交易参数"""
        trading_config = self.config.get('trading', {})
        self.trailing_stop_ratio = trading_config.get('trailing_stop_ratio', 0.08)
        self.hard_stop_loss = trading_config.get('hard_stop_loss', 0.10)
        self.min_hold_days = trading_config.get('min_hold_days', 5)
        self.max_hold_days = trading_config.get('max_hold_days', 30)
        self.commission = trading_config.get('commission', 0.00025)
        self.stamp_tax = trading_config.get('stamp_tax', 0.001)
        self.slippage = trading_config.get('slippage', 0.001)
        self.min_avg_volume = trading_config.get('min_avg_volume', 20_000_000)
        self.volume_check_period = trading_config.get('volume_check_period', 20)

    def check_all_patterns(self, df: pd.DataFrame, code: str,
                          period_start: str, period_end: str) -> List[Dict]:
        """
        检测所有符合条件的形态

        Args:
            df: K线数据
            code: 股票代码
            period_start: 开始日期
            period_end: 结束日期

        Returns:
            形态列表
        """
        # TODO: 实现形态检测逻辑
        # 这里应该从backtester_v24.py中复制核心检测逻辑
        pass

    def check_liquidity(self, df, retrace_idx):
        """检查股票流动性"""
        if retrace_idx < self.volume_check_period:
            return False

        start_idx = retrace_idx - self.volume_check_period
        end_idx = retrace_idx + 1

        recent_df = df.iloc[start_idx:end_idx].copy()

        if 'amount' in recent_df.columns:
            avg_amount = recent_df['amount'].mean()
        else:
            recent_amount = recent_df['volume'] * recent_df['close']
            avg_amount = recent_amount.mean()

        return avg_amount >= self.min_avg_volume

    def get_version_info(self) -> Dict:
        """获取当前版本信息"""
        return {
            'version': self.version,
            'drop_threshold': self.drop_threshold,
            'surge_price_ratio': self.surge_price_ratio,
            'min_volume_ratio': self.min_volume_ratio,
            'max_volume_ratio': self.max_volume_ratio,
            'volume_shrink_ratio': self.volume_shrink_ratio,
            'shrink_to_surge_days': self.shrink_to_surge_days,
        }
```

**步骤3: 创建core目录**

```bash
mkdir -p core
touch core/__init__.py
```

**验收标准**:
- [ ] 统一回测器类创建完成
- [ ] 支持版本切换
- [ ] 核心功能保留

#### 2.2.2 归档历史回测器(第3天)

**任务描述**: 将历史版本的回测器移动到归档目录。

**执行步骤**:

```bash
# 1. 移动历史回测器到归档目录
mv backtester_v22.py archived/backtesters/
mv backtester_v23.py archived/backtesters/
mv backtester_v25.py archived/backtesters/
mv backtester_v25_relaxed.py archived/backtesters/
mv backtester_scheme_ab.py archived/backtesters/
mv backtester_with_rules.py archived/backtesters/

# 2. 保留V2.4回测器作为参考
cp backtester_v24.py archived/backtesters/backtester_v24_reference.py

# 3. 创建归档说明
cat > archived/backtesters/README.md << 'EOF'
# 历史回测器归档

本目录存放历史版本的回测器代码,仅供参考。

## 文件说明

- `backtester_v22.py`: V2.2版本回测器
- `backtester_v23.py`: V2.3版本回测器
- `backtester_v24_reference.py`: V2.4版本回测器(参考)
- `backtester_v25.py`: V2.5版本回测器
- `backtester_v25_relaxed.py`: V2.5放宽版回测器
- `backtester_scheme_ab.py`: 方案AB回测器
- `backtester_with_rules.py`: 带规则的回测器

## 注意事项

1. 这些文件已不再使用,仅供参考
2. 如需查看历史版本逻辑,请参考这些文件
3. 新功能开发请使用 `core/backtester.py`
EOF
```

**验收标准**:
- [ ] 历史回测器已归档
- [ ] 归档说明已创建

#### 2.2.3 整理回测脚本(第4天)

**任务描述**: 整理回测脚本,只保留核心脚本。

**执行步骤**:

**步骤1: 创建scripts目录**

```bash
mkdir -p scripts
```

**步骤2: 创建核心回测脚本**

创建文件 `scripts/backtest_full.py`:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
全量回测脚本:
- 支持指定版本参数
- 支持指定时间范围
- 支持指定股票列表
"""

import argparse
import sys
import os

# 添加项目根目录到路径
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from core.backtester import Backtester


def main():
    parser = argparse.ArgumentParser(description='全量回测脚本')
    parser.add_argument('--version', type=str, default='v24',
                       choices=['v22', 'v23', 'v24', 'v25'],
                       help='回测版本')
    parser.add_argument('--start-date', type=str, default='2019-01-01',
                       help='开始日期')
    parser.add_argument('--end-date', type=str, default='2026-04-27',
                       help='结束日期')
    parser.add_argument('--config', type=str, default='config/config.yaml',
                       help='配置文件路径')
    parser.add_argument('--output', type=str, default='backtest_results/',
                       help='输出目录')

    args = parser.parse_args()

    print(f"开始全量回测...")
    print(f"版本: {args.version}")
    print(f"时间范围: {args.start_date} ~ {args.end_date}")

    # 创建回测器
    backtester = Backtester(config_path=args.config, version=args.version)

    # 打印版本信息
    version_info = backtester.get_version_info()
    print(f"版本参数: {version_info}")

    # TODO: 实现全量回测逻辑
    # 这里应该从backtest_v24_full.py中复制核心逻辑

    print("全量回测完成!")


if __name__ == '__main__':
    main()
```

**步骤3: 归档历史脚本**

```bash
# 移动历史回测脚本到归档目录
mv backtest_v21.py archived/scripts/
mv backtest_v21_final.py archived/scripts/
mv backtest_v21_full.py archived/scripts/
mv backtest_v21_final_report.py archived/scripts/
mv backtest_v22.py archived/scripts/
mv backtest_v22_relaxed.py archived/scripts/
mv backtest_v23.py archived/scripts/
mv backtest_v24_full.py archived/scripts/
mv backtest_v25_full.py archived/scripts/
mv backtest_603529_v24_vs_v25.py archived/scripts/
mv backtest_baostock_full.py archived/scripts/
mv compare_v21_v22.py archived/scripts/
mv compare_v24_v25.py archived/scripts/
mv compare_v24_v25_results.py archived/scripts/
mv comprehensive_analysis_v24_v25.py archived/scripts/

# 移动分析脚本到归档目录
mv analyze_v22.py archived/scripts/
mv analyze_v24_results.py archived/scripts/
mv analyze_main_board_klines.py archived/scripts/
mv check_603529_detailed.py archived/scripts/
mv check_603529_pattern.py archived/scripts/
mv check_603529_v22.py archived/scripts/
mv check_603529_v24_final.py archived/scripts/
mv check_603529_v24_with_server.py archived/scripts/
mv check_db_klines.py archived/scripts/
mv check_db_stats.py archived/scripts/
mv check_kline_data.py archived/scripts/
mv check_sync_status.py archived/scripts/
mv check_today.py archived/scripts/
mv clear_klines.py archived/scripts/

# 创建归档说明
cat > archived/scripts/README.md << 'EOF'
# 历史脚本归档

本目录存放历史版本的脚本文件,仅供参考。

## 文件分类

### 回测脚本
- `backtest_v21*.py`: V2.1版本回测脚本
- `backtest_v22*.py`: V2.2版本回测脚本
- `backtest_v23.py`: V2.3版本回测脚本
- `backtest_v24_full.py`: V2.4版本全量回测脚本
- `backtest_v25_full.py`: V2.5版本全量回测脚本

### 对比脚本
- `compare_v21_v22.py`: V2.1 vs V2.2对比
- `compare_v24_v25*.py`: V2.4 vs V2.5对比

### 分析脚本
- `analyze_*.py`: 各种分析脚本
- `check_*.py`: 各种检查脚本

## 注意事项

1. 这些脚本已不再使用,仅供参考
2. 新功能开发请使用 `scripts/` 目录下的脚本
EOF
```

**验收标准**:
- [ ] 核心回测脚本创建完成
- [ ] 历史脚本已归档
- [ ] 归档说明已创建

---

### 2.3 阶段3: 配置整理(第5天)

#### 2.3.1 创建统一配置文件

**任务描述**: 创建统一的配置文件,支持版本切换。

**执行步骤**:

**步骤1: 创建config目录**

```bash
mkdir -p config
```

**步骤2: 创建统一配置文件**

创建文件 `config/config.yaml`:

```yaml
# 股票形态筛选系统配置文件
# 版本: V2.4
# 最后更新: 2026-04-27

# 当前使用的版本
current_version: "v24"

# 版本参数配置
versions:
  v22:
    pattern:
      drop_threshold: 0.12          # 跌幅阈值 12%
      surge_price_ratio: 0.05       # 放量涨幅 5%
      min_volume_ratio: 1.5         # 最小量比 1.5倍
      max_volume_ratio: 12.0        # 最大量比 12倍
      volume_shrink_ratio: 0.6      # 缩量比例 60%
      shrink_to_surge_days: 10      # 缩量到放量时间窗口 10天

  v23:
    pattern:
      drop_threshold: 0.10          # 跌幅阈值 10%
      surge_price_ratio: 0.04       # 放量涨幅 4%
      min_volume_ratio: 1.3         # 最小量比 1.3倍
      max_volume_ratio: 13.0        # 最大量比 13倍
      volume_shrink_ratio: 0.7      # 缩量比例 70%
      shrink_to_surge_days: 20      # 缩量到放量时间窗口 20天

  v24:
    pattern:
      drop_threshold: 0.08          # 跌幅阈值 8%
      surge_price_ratio: 0.03       # 放量涨幅 3%
      min_volume_ratio: 1.2         # 最小量比 1.2倍
      max_volume_ratio: 15.0        # 最大量比 15倍
      volume_shrink_ratio: 0.8      # 缩量比例 80%
      shrink_to_surge_days: 60      # 缩量到放量时间窗口 60天

  v25:
    pattern:
      drop_threshold: 0.09          # 跌幅阈值 9%
      surge_price_ratio: 0.035      # 放量涨幅 3.5%
      min_volume_ratio: 1.3         # 最小量比 1.3倍
      max_volume_ratio: 15.0        # 最大量比 15倍
      volume_shrink_ratio: 0.8      # 缩量比例 80%
      shrink_to_surge_days: 50      # 缩量到放量时间窗口 50天

# 当前版本配置(根据current_version自动加载)
pattern:
  drop_threshold: 0.08              # 跌幅阈值 8%
  surge_price_ratio: 0.03           # 放量涨幅 3%
  min_volume_ratio: 1.2             # 最小量比 1.2倍
  max_volume_ratio: 15.0            # 最大量比 15倍
  volume_shrink_ratio: 0.8          # 缩量比例 80%
  shrink_to_surge_days: 60          # 缩量到放量时间窗口 60天

  # 方案B参数
  flat_days: 0                      # 取消缩量走平(0=取消)
  post_surge_check_days: 5          # 启动后观察 5天
  post_surge_max_drop: 0.97         # 最低不低于启动价 97%

# 交易参数
trading:
  # 止盈止损
  trailing_stop_ratio: 0.08         # 移动止盈 8%
  hard_stop_loss: 0.10              # 硬止损 10%
  min_hold_days: 5                  # 最短持仓 5天
  max_hold_days: 30                 # 最长持仓 30天

  # 交易成本
  commission: 0.00025               # 佣金 0.025%
  stamp_tax: 0.001                  # 印花税 0.1%
  slippage: 0.001                   # 滑点 0.1%

  # 流动性过滤
  min_avg_volume: 20_000_000        # 日均成交额≥2000万
  volume_check_period: 20           # 统计 20日均值

  # 买入规则
  entry_timing: next_open           # 次日开盘买入
  skip_high_open_threshold: 0.05    # 高开>5% 跳过
  skip_limit_up_open: true          # 涨停开盘跳过

# 数据库配置
database:
  host: localhost
  port: 5432
  database: stockfilter
  user: postgres
  password: your_password

# 飞书推送配置
feishu:
  webhook_url: "https://open.feishu.cn/open-apis/bot/v2/hook/your_webhook_url"
  enabled: true

# 日志配置
logging:
  level: INFO
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  file: logs/stockfilter.log
```

**步骤3: 归档历史配置文件**

```bash
# 移动历史配置文件到归档目录
mv config_v21_final.yaml archived/configs/
mv config_v22_relaxed.yaml archived/configs/
mv config_v24_final.yaml archived/configs/

# 创建归档说明
cat > archived/configs/README.md << 'EOF'
# 历史配置文件归档

本目录存放历史版本的配置文件,仅供参考。

## 文件说明

- `config_v21_final.yaml`: V2.1最终配置
- `config_v22_relaxed.yaml`: V2.2放宽配置
- `config_v24_final.yaml`: V2.4最终配置

## 注意事项

1. 这些配置文件已不再使用,仅供参考
2. 新项目请使用 `config/config.yaml`
EOF
```

**验收标准**:
- [ ] 统一配置文件创建完成
- [ ] 历史配置文件已归档
- [ ] 配置加载正常

---

### 2.4 阶段4: 文档整理(第6-7天)

#### 2.4.1 优化文档目录结构(第6天)

**任务描述**: 优化docs目录结构,建立清晰的文档分类。

**执行步骤**:

```bash
# 1. 创建新的文档目录结构
mkdir -p docs/current
mkdir -p docs/archived/v21
mkdir -p docs/archived/v22
mkdir -p docs/archived/v23
mkdir -p docs/references

# 2. 移动当前版本文档
# (保留V2.4相关文档在docs/current/)

# 3. 移动历史版本文档
mv docs/reports/V2.1*.md docs/archived/v21/
mv docs/schemes/V2.1*.md docs/archived/v21/
mv docs/schemes/策略配置方案V2.0.md docs/archived/v21/

# 4. 移动根目录下的临时文档
mv V21_回测数据说明.md docs/archived/v21/
mv V21_回测触发时间统计.md docs/archived/v21/
mv V21_完整回测报告_简化版.md docs/archived/v21/

# 5. 更新文档索引
cat > docs/README.md << 'EOF'
# 项目文档索引

本文档是项目文档的统一索引,所有文档按类别存储在以下目录中。

## 📁 目录结构

```
docs/
├── README.md              # 本文档(文档索引)
├── current/               # 当前版本文档(V2.4)
│   ├── 需求文档.md
│   ├── 架构设计.md
│   ├── 部署指南.md
│   └── 使用手册.md
├── archived/              # 历史版本文档
│   ├── v21/              # V2.1版本文档
│   ├── v22/              # V2.2版本文档
│   └── v23/              # V2.3版本文档
├── requirements/          # 需求类文档
├── reports/               # 报告类文档
├── deployment/            # 部署类文档
├── schemes/               # 方案类文档
├── designs/               # 设计类文档
└── references/            # 参考资料
```

## 📋 当前版本文档 (current/)

| 文档名称 | 说明 | 状态 |
|---------|------|------|
| [需求文档.md](current/需求文档.md) | V2.4需求文档 | ✅ 当前 |
| [架构设计.md](current/架构设计.md) | V2.4架构设计 | ✅ 当前 |
| [部署指南.md](current/部署指南.md) | V2.4部署指南 | ✅ 当前 |
| [使用手册.md](current/使用手册.md) | V2.4使用手册 | ✅ 当前 |

## 📊 报告类文档 (reports/)

记录项目各阶段的报告、总结和分析。

| 文档名称 | 说明 |
|---------|------|
| [V2.4 策略优化完成报告.md](reports/V2.4%20策略优化完成报告.md) | **V2.4 策略优化周期总结报告(最新)** |
| [V2.4 vs V2.5 全方位收益对比报告.md](reports/V2.4%20vs%20V2.5%20全方位收益对比报告.md) | **V2.4 与 V2.5 全方位收益对比分析** |

## 📋 方案类文档 (schemes/)

策略配置方案、执行计划等。

| 文档名称 | 说明 |
|---------|------|
| [V2.4 最终配置方案.md](schemes/V2.4%20最终配置方案.md) | **V2.4 最终配置方案(推荐使用)** |

## 🚀 部署类文档 (deployment/)

部署指南、操作手册和运维文档。

| 文档名称 | 说明 |
|---------|------|
| [服务器部署指南.md](deployment/服务器部署指南.md) | 服务器 Docker 部署完整指南 |
| [飞书推送使用手册.md](deployment/飞书推送使用手册.md) | 飞书推送系统配置与使用手册 |

## 🎨 设计类文档 (designs/)

系统设计、技术架构和监控指南。

| 文档名称 | 说明 |
|---------|------|
| [技术架构文档 V2.1.md](designs/技术架构文档_V2.1.md) | 系统完整技术架构设计文档 |

## 📝 文档管理规范

### 文档分类原则

- **current/**: 当前版本的最新文档
- **archived/**: 历史版本的归档文档
- **requirements/**: 项目业务需求、功能规格、版本迭代记录
- **reports/**: 项目阶段性报告、总结、分析、验证报告
- **deployment/**: 部署指南、操作手册、运维文档、使用手册
- **schemes/**: 策略配置方案、执行计划、实施方案
- **designs/**: 系统设计、技术架构、监控指南、规范文档
- **references/**: 参考资料、技术选型文档

### 文档命名规范

- 使用中文命名,清晰表达文档内容
- 版本信息使用 `Vx.x` 格式
- 日期信息使用 `YYYY-MM-DD` 格式
- 避免使用空格,使用下划线或连字符分隔

---

**最后更新**: 2026-04-27
**文档版本**: V2.0(重构后版本)
EOF
```

**验收标准**:
- [ ] 文档目录结构优化完成
- [ ] 历史文档已归档
- [ ] 文档索引已更新

#### 2.4.2 清理重复文档(第7天)

**任务描述**: 清理重复和过时的文档。

**执行步骤**:

```bash
# 1. 识别重复文档
# (手动检查docs目录下的文档,找出重复内容)

# 2. 删除或归档重复文档
# (根据实际情况决定删除或归档)

# 3. 更新文档索引
# (更新docs/README.md)
```

**验收标准**:
- [ ] 重复文档已清理
- [ ] 文档索引已更新

---

### 2.5 阶段5: 项目结构优化(第8-9天)

#### 2.5.1 优化目录结构(第8天)

**任务描述**: 优化项目根目录结构,建立清晰的模块划分。

**执行步骤**:

```bash
# 1. 创建新的目录结构
mkdir -p core
mkdir -p analysis
mkdir -p scripts
mkdir -p config

# 2. 移动核心代码
# (已经在前面步骤完成)

# 3. 移动脚本文件
mv daily_scan.py scripts/
mv feishu_push.py scripts/

# 4. 更新导入路径
# (需要修改相关文件的导入路径)

# 5. 更新README.md
cat > README.md << 'EOF'
# 股票形态筛选系统 V2.4

基于"大跌企稳、缩量见底、放量启动、回踩确认"形态的自动化股票筛选系统。

**最新版本**: V2.4(最终版 - 20260414)

## 项目结构

```
stockfilter/
├── core/                        # 核心业务逻辑
│   ├── backtester.py           # 回测器
│   ├── pattern_detector.py     # 形态检测
│   └── signal_generator.py     # 信号生成
├── analysis/                    # 分析工具
│   ├── db_checker.py           # 数据库检查
│   └── kline_analyzer.py       # K线分析
├── data/                        # 数据管理
│   ├── database.py             # 数据库管理
│   ├── data_source.py          # 数据源
│   └── stock_list.py           # 股票列表
├── trading/                     # 交易相关
│   └── backtest.py             # 回测框架
├── scripts/                     # 脚本工具
│   ├── backtest_full.py        # 全量回测
│   ├── backtest_single.py      # 单只股票回测
│   └── daily_scan.py           # 每日扫描
├── config/                      # 配置文件
│   └── config.yaml             # 主配置文件
├── docs/                        # 文档
├── archived/                    # 归档文件
├── tests/                       # 测试代码
├── signals/                     # 信号输出
├── logs/                        # 日志文件
└── README.md                    # 项目说明
```

## 快速开始

### 1. 环境准备

```bash
# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Mac/Linux

# 安装依赖
pip install -r requirements.txt
```

### 2. 运行扫描

```bash
# 执行形态扫描
python scripts/daily_scan.py

# 发送飞书推送
python scripts/feishu_push.py
```

### 3. 运行回测

```bash
# 全量回测(默认V2.4版本)
python scripts/backtest_full.py

# 指定版本回测
python scripts/backtest_full.py --version v22

# 指定时间范围
python scripts/backtest_full.py --start-date 2020-01-01 --end-date 2025-12-31
```

## 文档

- [完整文档索引](docs/README.md)
- [需求文档](docs/requirements/股票形态筛选系统重构需求文档.md)
- [部署指南](docs/deployment/服务器部署指南.md)
- [技术架构](docs/designs/技术架构文档_V2.1.md)

## 许可证

MIT License
EOF
```

**验收标准**:
- [ ] 目录结构优化完成
- [ ] 核心代码已移动
- [ ] README.md已更新

#### 2.5.2 更新导入路径(第9天)

**任务描述**: 更新所有文件的导入路径,确保代码正常运行。

**执行步骤**:

```bash
# 1. 查找所有需要更新的导入路径
grep -r "from backtester" --include="*.py" .
grep -r "import backtester" --include="*.py" .

# 2. 批量更新导入路径
# (使用sed或其他工具批量替换)

# 3. 测试所有功能
python scripts/daily_scan.py --test
python scripts/backtest_full.py --test
```

**验收标准**:
- [ ] 所有导入路径已更新
- [ ] 所有功能正常运行

---

### 2.6 阶段6: 测试验证(第10-11天)

#### 2.6.1 运行所有测试(第10天)

**任务描述**: 运行所有测试用例,确保功能完整。

**执行步骤**:

```bash
# 1. 运行单元测试
python -m pytest tests/unit/ -v

# 2. 运行集成测试
python -m pytest tests/integration/ -v

# 3. 运行功能测试
python scripts/daily_scan.py --test
python scripts/backtest_full.py --test
```

**验收标准**:
- [ ] 所有单元测试通过
- [ ] 所有集成测试通过
- [ ] 所有功能测试通过

#### 2.6.2 代码质量检查(第11天)

**任务描述**: 检查代码质量,确保符合规范。

**执行步骤**:

```bash
# 1. 代码复杂度检查
radon cc core/ -a

# 2. 代码重复率检查
radon raw core/ -s

# 3. 代码风格检查
flake8 core/ --max-line-length=120

# 4. 测试覆盖率检查
pytest --cov=core tests/
```

**验收标准**:
- [ ] 代码复杂度符合要求
- [ ] 代码重复率符合要求
- [ ] 代码风格符合规范
- [ ] 测试覆盖率符合要求

---

### 2.7 阶段7: 上线部署(第12天)

#### 2.7.1 合并重构分支

**任务描述**: 将重构分支合并到主分支。

**执行步骤**:

```bash
# 1. 切换到主分支
git checkout main

# 2. 合并重构分支
git merge refactor/cleanup-project-structure

# 3. 推送到远程
git push origin main

# 4. 删除重构分支
git branch -d refactor/cleanup-project-structure
git push origin --delete refactor/cleanup-project-structure
```

**验收标准**:
- [ ] 重构分支已合并
- [ ] 代码已推送到远程

#### 2.7.2 部署到生产环境

**任务描述**: 将重构后的代码部署到生产环境。

**执行步骤**:

```bash
# 1. 更新部署文档
# (更新docs/deployment/服务器部署指南.md)

# 2. 部署到测试环境
# (执行部署脚本)

# 3. 验证测试环境
# (运行测试用例)

# 4. 部署到生产环境
# (执行部署脚本)

# 5. 验证生产环境
# (运行测试用例)
```

**验收标准**:
- [ ] 部署文档已更新
- [ ] 测试环境部署成功
- [ ] 生产环境部署成功
- [ ] 所有功能正常

---

## 3. 风险控制

### 3.1 风险识别与应对

| 风险项 | 风险等级 | 应对措施 | 责任人 |
|--------|----------|----------|--------|
| 重构破坏现有功能 | 高 | 充分测试,保留回滚能力 | 开发团队 |
| 配置迁移失败 | 中 | 保留原配置,逐步迁移 | 开发团队 |
| 文档丢失 | 低 | 备份文档,归档历史版本 | 文档团队 |
| 团队适应困难 | 中 | 提供培训,编写迁移指南 | 项目经理 |

### 3.2 回滚计划

**回滚触发条件**:
- 核心功能无法正常运行
- 测试用例失败率超过20%
- 性能下降超过30%

**回滚步骤**:
1. 停止重构分支的合并
2. 恢复到重构前的代码版本
3. 分析失败原因
4. 修复问题后重新开始重构

---

## 4. 验收标准

### 4.1 功能验收

- [ ] 所有现有功能正常运行
- [ ] 所有测试用例通过
- [ ] 性能不低于重构前

### 4.2 代码质量验收

- [ ] 代码复杂度≤10
- [ ] 代码重复率≤5%
- [ ] 测试覆盖率≥当前水平

### 4.3 文档验收

- [ ] 文档目录结构清晰
- [ ] 文档索引完整准确
- [ ] 每个文档都有版本标识

### 4.4 部署验收

- [ ] 部署文档更新完成
- [ ] 测试环境部署成功
- [ ] 生产环境部署成功

---

## 5. 后续计划

### 5.1 短期计划(1个月内)

1. 监控重构效果
2. 收集团队反馈
3. 持续优化项目结构

### 5.2 中期计划(3个月内)

1. 完善测试体系
2. 优化性能
3. 扩展新功能

### 5.3 长期计划(6个月内)

1. 架构升级
2. 技术选型评估
3. 团队培训

---

**文档结束**
