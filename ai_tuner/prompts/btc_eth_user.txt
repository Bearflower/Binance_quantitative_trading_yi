请对 {{ strategy_name }} 进行周度调优分析。

## 当前策略配置

```json
{{ current_params }}
```

**【重要】** 只有以上 `current_params` 中列出的参数可以调整。**严禁引入以下红线参数（禁止调整）：**
- `binance.leverage.*`（各等级杠杆）
- `binance.position_ratio.*`（各等级仓位比例）

## 本周体检报告

```json
{{ report }}
```

## 历史调优记忆

{{ memory_history }}

## 上次调优效果追踪

{{ feedback_context }}

请基于以上数据，给出本周的参数调优建议。严格按照 JSON 格式输出。