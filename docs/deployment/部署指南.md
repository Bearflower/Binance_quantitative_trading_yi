# 统一交易系统部署指南

**文档版本**: v1.0
**最后更新**: 2026-05-05
**作者**: 需求文档专家
**审核人**: 待定

---

## 文档修订历史

| 版本 | 日期 | 修改人 | 修改内容 | 审核人 |
|------|------|--------|----------|--------|
| v1.0 | 2026-05-05 | 需求文档专家 | 初始版本创建 | 待定 |

---

## 1. 部署概述

### 1.1 文档目的

本文档提供统一交易系统的完整部署指南,包括部署前准备、部署流程、部署验证和故障排查,确保系统能够顺利部署到生产环境并稳定运行。

### 1.2 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     生产服务器 (43.156.242.184)                  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Docker 容器集群                              │  │
│  │                                                            │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐               │  │
│  │  │ BTC/ETH  │  │   Grid   │  │ New Coin │               │  │
│  │  │  容器    │  │   容器   │  │   容器   │               │  │
│  │  └──────────┘  └──────────┘  └──────────┘               │  │
│  │                                                            │  │
│  │  ┌──────────────────────────────────────────────┐       │  │
│  │  │          共享核心模块 (Python包)              │       │  │
│  │  └──────────────────────────────────────────────┘       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              PostgreSQL 数据库 (已有服务)                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              通用服务 (K线服务、通知服务)                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 部署策略

采用**渐进式部署**策略:

1. **第一阶段**: 部署共享核心模块和数据库
2. **第二阶段**: 部署BTC/ETH/BNB策略(优先)
3. **第三阶段**: 部署其他策略(Grid、New Coin等)
4. **第四阶段**: 监控和优化

### 1.4 部署环境

| 环境 | 服务器IP | 用途 | 说明 |
|------|----------|------|------|
| 生产环境 | 43.156.242.184 | 正式运行 | 已有PostgreSQL、K线服务、通知服务 |

---

## 2. 部署前准备

### 2.1 服务器要求

#### 2.1.1 硬件要求

| 资源 | 最低配置 | 推荐配置 | 当前配置 |
|------|----------|----------|----------|
| CPU | 2核 | 4核 | 4核 |
| 内存 | 4GB | 8GB | 8GB |
| 磁盘 | 50GB | 100GB | 100GB |
| 网络 | 10Mbps | 100Mbps | 100Mbps |

#### 2.1.2 软件要求

| 软件 | 版本要求 | 检查命令 | 安装命令 |
|------|----------|----------|----------|
| Docker | >= 20.10 | `docker --version` | `curl -fsSL https://get.docker.com \| sh` |
| Docker Compose | >= 2.0 | `docker-compose --version` | `pip install docker-compose` |
| Git | >= 2.30 | `git --version` | `apt-get install git` |
| PostgreSQL | >= 14 | `psql --version` | 已安装 |

#### 2.1.3 网络要求

| 端口 | 用途 | 访问控制 |
|------|------|----------|
| 22 | SSH | 仅允许开发机访问 |
| 5432 | PostgreSQL | 仅Docker网络访问 |
| 8765 | K线服务 | 仅Docker网络访问 |
| 8766 | 通知服务 | 仅Docker网络访问 |

### 2.2 SSH免密登录配置

**重要**: 必须先配置SSH免密登录,这是部署的前提条件!

#### 2.2.1 使用云平台密钥(推荐)

**步骤1: 确认密钥文件**

```bash
# 检查密钥文件是否存在
ls -la /Users/yl/vscode/inspection_automation/docs/only.pem

# 设置密钥文件权限(必须)
chmod 600 /Users/yl/vscode/inspection_automation/docs/only.pem

# 验证权限
ls -la /Users/yl/vscode/inspection_automation/docs/only.pem
# 应该显示: -rw------- 1 yl staff ... only.pem
```

**步骤2: 测试密钥登录**

```bash
# 测试登录(不需要输入密码)
ssh -i /Users/yl/vscode/inspection_automation/docs/only.pem \
    -o StrictHostKeyChecking=no \
    -o UserKnownHostsFile=/dev/null \
    root@43.156.242.184 "echo 'SSH密钥登录成功!'"

# 如果成功,会直接返回"SSH密钥登录成功!",不需要输入密码
```

**步骤3: 配置SSH别名(推荐)**

编辑 `~/.ssh/config` 文件:

```bash
cat >> ~/.ssh/config << 'EOF'

# 统一交易系统生产服务器
Host trading-prod
    HostName 43.156.242.184
    User root
    IdentityFile /Users/yl/vscode/inspection_automation/docs/only.pem
    IdentitiesOnly yes
    StrictHostKeyChecking no
    UserKnownHostsFile=/dev/null
    AddKeysToAgent yes
    ServerAliveInterval 60
    ServerAliveCountMax 3
EOF
```

**使用别名登录:**

```bash
ssh trading-prod
```

### 2.3 服务器目录结构规范

遵循服务器自动化部署技能的规范要求:

```
/root/
├── database/                 # 数据库目录(统一管理)
│   └── postgres/             # PostgreSQL 数据库(已有)
│
├── Binance_quantitative_trading/  # 统一交易系统项目目录
│   ├── src/                  # 源代码
│   ├── config/               # 配置文件
│   ├── logs/                 # 日志文件
│   ├── data/                 # 数据文件
│   ├── shared/               # 共享核心模块
│   ├── strategies/           # 策略模块
│   ├── docker-compose.yml    # Docker编排
│   ├── .env                  # 环境变量
│   └── .deploy_config        # 部署配置
│
└── backup/                   # 备份目录
    └── trading_backup_YYYYMMDD.tar.gz
```

**重要原则:**

1. **单一目录原则**: 一个项目在 `/root` 下只能有一个子目录
2. **单一容器原则**: 一个项目在Docker中只能有一个运行中的容器实例
3. **规范命名原则**: 目录和容器命名必须规范、清晰

### 2.4 数据库准备

#### 2.4.1 检查PostgreSQL服务

```bash
# 检查PostgreSQL容器状态
ssh trading-prod "docker ps -f name=postgres-db"

# 如果未运行,启动PostgreSQL
ssh trading-prod "docker-compose -f /root/database/postgres/docker-compose.yml up -d"
```

#### 2.4.2 创建数据库和Schema

```bash
# 连接到PostgreSQL
ssh trading-prod "docker exec -it postgres-db psql -U postgres"

# 在psql中执行
CREATE DATABASE trading_platform;
CREATE USER trading_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE trading_platform TO trading_user;

# 切换到trading_platform数据库
\c trading_platform

# 创建Schema
CREATE SCHEMA IF NOT EXISTS trading;
GRANT ALL PRIVILEGES ON SCHEMA trading TO trading_user;

# 退出
\q
```

#### 2.4.3 初始化表结构

```bash
# 上传数据库初始化脚本
scp -i /Users/yl/vscode/inspection_automation/docs/only.pem \
    /Users/yl/vscode/Binance_quantitative_trading/docs/architecture/database_schema.sql \
    root@43.156.242.184:/tmp/

# 执行初始化脚本
ssh trading-prod "docker exec -i postgres-db psql -U trading_user -d trading_platform < /tmp/database_schema.sql"
```

### 2.5 通用服务检查

#### 2.5.1 检查K线服务

```bash
# 检查K线服务状态
ssh trading-prod "docker ps -f name=kline-service"

# 测试K线服务接口
ssh trading-prod "curl http://localhost:8765/api/v1/health"

# 预期返回: {"code":0,"message":"success","data":{"status":"healthy"}}
```

#### 2.5.2 检查通知服务

```bash
# 检查通知服务状态
ssh trading-prod "docker ps -f name=notification-service"

# 测试通知服务接口
ssh trading-prod "curl http://localhost:8766/api/v1/health"

# 预期返回: {"code":0,"message":"success","data":{"status":"healthy"}}
```

### 2.6 本地环境准备

#### 2.6.1 安装必要工具

```bash
# macOS安装rsync
brew install rsync

# 验证安装
rsync --version
```

#### 2.6.2 创建部署配置文件

在项目根目录创建 `.deploy_config` 文件:

```bash
cat > /Users/yl/vscode/Binance_quantitative_trading/.deploy_config << 'EOF'
# 服务器配置
SERVER_IP="43.156.242.184"
SERVER_USER="root"
SERVER_PROJECT_PATH="/root/Binance_quantitative_trading"

# Docker配置
DOCKER_CONTAINER_NAME="trading-system"
DOCKER_IMAGE_NAME="trading-system:latest"

# 项目配置
PROJECT_NAME="Binance_quantitative_trading"
DEPLOY_PACKAGE_NAME="deployment_package.tar.gz"
EOF
```

---

## 3. 部署流程

### 3.1 标准部署流程

#### 3.1.1 步骤1: 部署前检查

```bash
# 进入项目目录
cd /Users/yl/vscode/Binance_quantitative_trading

# 检查项目文件
ls -la

# 检查配置文件
cat .env
cat .deploy_config

# 检查SSH连接
ssh trading-prod "echo 'SSH连接正常'"

# 检查服务器磁盘空间
ssh trading-prod "df -h"
```

#### 3.1.2 步骤2: 创建自动打包脚本

创建 `auto_package.sh`:

```bash
#!/bin/bash

# ============================================
# 自动化打包脚本(增强版 - 防止文件遗漏)
# ============================================

set -e  # 遇到错误立即退出

# 加载配置
if [ -f ".deploy_config" ]; then
    source .deploy_config
    echo "✅ 已加载部署配置"
else
    echo "❌ 错误：.deploy_config 文件不存在"
    exit 1
fi

# 设置默认值
DEPLOY_PACKAGE_NAME=${DEPLOY_PACKAGE_NAME:-"deployment_package.tar.gz"}
PROJECT_NAME=${PROJECT_NAME:-$(basename "$(pwd)")}

echo "============================================="
echo "开始打包项目：$PROJECT_NAME"
echo "============================================="

# 创建临时目录
TEMP_DIR="/tmp/${PROJECT_NAME}_deploy_$$"
echo "📁 创建临时目录：$TEMP_DIR"
mkdir -p "$TEMP_DIR"

# 复制项目文件(排除不需要的文件)
echo "📋 复制项目文件..."
rsync -av --delete \
    --exclude='*.pyc' \
    --exclude='__pycache__' \
    --exclude='*.pyo' \
    --exclude='.git' \
    --exclude='.gitignore' \
    --exclude='logs/*' \
    --exclude='data/*' \
    --exclude='reports/*' \
    --exclude='*.tar.gz' \
    --exclude='.DS_Store' \
    --exclude='._*' \
    --exclude='node_modules/*' \
    --exclude='.env.local' \
    --exclude='.trae/*' \
    --exclude='*.log' \
    --exclude='tmp/*' \
    --exclude='.pytest_cache' \
    --exclude='.mypy_cache' \
    --exclude='.coverage' \
    --exclude='htmlcov/*' \
    ./ "$TEMP_DIR/"

# 创建压缩包
echo "📦 创建压缩包..."
cd "$TEMP_DIR"
tar -czf "$OLDPWD/$DEPLOY_PACKAGE_NAME" .

# 清理临时目录
cd "$OLDPWD"
rm -rf "$TEMP_DIR"

# 显示结果
PACKAGE_SIZE=$(ls -lh "$DEPLOY_PACKAGE_NAME" | awk '{print $5}')
echo "============================================="
echo "✅ 打包完成！"
echo "📦 压缩包：$DEPLOY_PACKAGE_NAME"
echo "📊 大小：$PACKAGE_SIZE"
echo "============================================="

# 文件完整性检查
echo ""
echo "🔍 执行文件完整性检查..."

# 统计本地文件数量(排除相同规则)
LOCAL_FILE_COUNT=$(find . -type f \
    ! -path './.git/*' \
    ! -path './logs/*' \
    ! -path './data/*' \
    ! -path './reports/*' \
    ! -path './node_modules/*' \
    ! -path './.pytest_cache/*' \
    ! -path './.mypy_cache/*' \
    ! -path './htmlcov/*' \
    ! -path './tmp/*' \
    ! -name '*.pyc' \
    ! -name '*.pyo' \
    ! -name '*.tar.gz' \
    ! -name '*.log' \
    ! -name '.DS_Store' \
    ! -name '._*' \
    ! -name '.env.local' \
    ! -path './.trae/*' \
    | wc -l)

# 解压压缩包并统计文件数量
VERIFY_DIR="/tmp/${PROJECT_NAME}_verify_$$"
mkdir -p "$VERIFY_DIR"
tar -xzf "$DEPLOY_PACKAGE_NAME" -C "$VERIFY_DIR"

PACKAGE_FILE_COUNT=$(find "$VERIFY_DIR" -type f | wc -l)
rm -rf "$VERIFY_DIR"

echo "📊 本地文件数量：$LOCAL_FILE_COUNT"
echo "📦 压缩包文件数量：$PACKAGE_FILE_COUNT"

# 计算差异
DIFF=$((LOCAL_FILE_COUNT - PACKAGE_FILE_COUNT))
if [ $DIFF -lt 0 ]; then
    DIFF=$((-DIFF))
fi

# 如果差异超过5个文件,发出警告
if [ $DIFF -gt 5 ]; then
    echo "⚠️  警告：文件数量差异较大（差异：$DIFF 个文件）"
    echo "   可能遗漏了文件，请检查排除规则！"
    read -p "是否继续？(y/N): " confirm
    if [ "$confirm" != "y" ] && [ "$confirm" != "Y" ]; then
        echo "❌ 打包已取消"
        exit 1
    fi
else
    echo "✅ 文件数量检查通过（差异：$DIFF 个文件，在允许范围内）"
fi

echo ""
echo "============================================="
echo "🎉 打包完成并通过完整性检查！"
echo "============================================="
```

**执行权限:**

```bash
chmod +x auto_package.sh
```

#### 3.1.3 步骤3: 创建上传脚本

创建 `upload_to_server.sh`:

```bash
#!/bin/bash

# ============================================
# 上传脚本 - 使用SSH密钥认证
# ============================================

set -e

# 加载配置
source .deploy_config

echo "============================================="
echo "上传到服务器：$SERVER_IP"
echo "============================================="

# 检查压缩包是否存在
if [ ! -f "$DEPLOY_PACKAGE_NAME" ]; then
    echo "❌ 错误：压缩包不存在，请先执行打包"
    exit 1
fi

# 使用SSH密钥认证
echo "🔑 使用SSH密钥认证..."

# 测试SSH密钥是否可用
if ssh -o StrictHostKeyChecking=no -o BatchMode=yes "$SERVER_USER@$SERVER_IP" "echo 密钥可用" 2>/dev/null; then
    echo "✅ SSH密钥可用，开始上传..."

    scp -o StrictHostKeyChecking=no \
        -o UserKnownHostsFile=/dev/null \
        "$DEPLOY_PACKAGE_NAME" \
        "$SERVER_USER@$SERVER_IP:/root/"

    echo "✅ 上传成功（SSH密钥）"
else
    echo "❌ 错误：SSH密钥不可用，请先配置免密登录"
    echo ""
    echo "请执行以下步骤："
    echo "1. 确保云平台密钥文件存在：/Users/yl/vscode/inspection_automation/docs/only.pem"
    echo "2. 设置密钥权限：chmod 600 /Users/yl/vscode/inspection_automation/docs/only.pem"
    echo "3. 测试密钥登录：ssh -i /Users/yl/vscode/inspection_automation/docs/only.pem root@$SERVER_IP 'echo 成功'"
    exit 1
fi

echo "============================================="
```

**执行权限:**

```bash
chmod +x upload_to_server.sh
```

#### 3.1.4 步骤4: 创建一键部署脚本

创建 `one_click_deploy.sh`:

```bash
#!/bin/bash

# ============================================
# 一键部署脚本(增强版 - 确保容器更新)
# ============================================

set -e

# 加载配置
source .deploy_config

echo "============================================="
echo "一键部署 - $PROJECT_NAME"
echo "目标服务器：$SERVER_IP"
echo "============================================="

# 步骤1：打包
echo "📦 步骤 1/5: 打包项目..."
./auto_package.sh

# 步骤2：上传
echo "📤 步骤 2/5: 上传到服务器..."
./upload_to_server.sh

# 步骤3：远程部署
echo "🚀 步骤 3/5: 远程部署..."

# 使用SSH执行远程部署命令
ssh -o StrictHostKeyChecking=no \
    -o UserKnownHostsFile=/dev/null \
    "$SERVER_USER@$SERVER_IP" << ENDSSH

# 在服务器上执行的命令
set -e

echo "============================================="
echo "远程部署开始"
echo "============================================="

# 1. 停止并删除旧容器
echo "🛑 停止旧容器..."
if docker ps -q -f name=$DOCKER_CONTAINER_NAME | grep -q .; then
    docker stop $DOCKER_CONTAINER_NAME
    echo "✅ 容器已停止"
else
    echo "⚠️  容器未运行，跳过停止"
fi

echo "🗑️  删除旧容器..."
if docker ps -aq -f name=$DOCKER_CONTAINER_NAME | grep -q .; then
    docker rm $DOCKER_CONTAINER_NAME
    echo "✅ 容器已删除"
else
    echo "⚠️  容器不存在，跳过删除"
fi

# 2. 删除旧镜像(关键步骤，防止使用缓存)
echo "🗑️  删除旧镜像（防止使用缓存）..."
if docker images -q $DOCKER_IMAGE_NAME | grep -q .; then
    docker rmi $DOCKER_IMAGE_NAME --force 2>/dev/null || true
    echo "✅ 旧镜像已删除"
else
    echo "⚠️  旧镜像不存在，跳过删除"
fi

# 3. 解压新包
echo "📦 解压新代码包..."
cd /root
mkdir -p $PROJECT_NAME
tar -xzf $DEPLOY_PACKAGE_NAME -C $PROJECT_NAME
echo "✅ 代码包已解压"

# 4. 设置权限
cd $PROJECT_NAME
chmod +x *.sh 2>/dev/null || true
chmod 600 .env 2>/dev/null || true
echo "✅ 权限已设置"

# 5. 清理Docker缓存(可选)
echo "🧹 清理Docker悬空镜像..."
docker image prune -f --filter "until=24h" 2>/dev/null || true

# 6. 构建并启动(不使用缓存)
echo "🏗️  构建Docker镜像（不使用缓存）..."
docker-compose build --no-cache
if [ \$? -ne 0 ]; then
    echo "❌ Docker构建失败！"
    exit 1
fi
echo "✅ Docker镜像构建成功"

echo "🚀 启动Docker容器..."
docker-compose up -d
if [ \$? -ne 0 ]; then
    echo "❌ Docker容器启动失败！"
    exit 1
fi
echo "✅ Docker容器启动成功"

# 7. 等待容器启动
echo "⏳ 等待容器启动..."
sleep 5

# 8. 显示状态
echo "============================================="
echo "容器状态:"
docker ps -f name=$DOCKER_CONTAINER_NAME
echo "============================================="
echo "最近日志:"
docker logs --tail 30 $DOCKER_CONTAINER_NAME
echo "============================================="
ENDSSH

# 检查远程部署是否成功
if [ $? -ne 0 ]; then
    echo "❌ 远程部署失败！"
    exit 1
fi

# 步骤4：验证部署
echo "✅ 步骤 4/5: 验证部署..."

# 创建验证脚本
cat > /tmp/verify_deployment.sh << 'VERIFYEOF'
#!/bin/bash

DOCKER_CONTAINER_NAME="$1"
PROJECT_NAME="$2"

echo "============================================="
echo "🔍 部署验证 - 确保容器更新到最新版本"
echo "============================================="

# 1. 验证容器是否在运行
echo "1️⃣  验证容器运行状态..."
CONTAINER_STATUS=$(docker ps -f name=$DOCKER_CONTAINER_NAME --format '{{.Status}}')
if [ -z "$CONTAINER_STATUS" ]; then
    echo "❌ 容器未运行！部署失败！"
    exit 1
fi
echo "✅ 容器运行状态：$CONTAINER_STATUS"

# 2. 验证容器镜像版本
echo ""
echo "2️⃣  验证容器镜像版本（确保是最新版本）..."
CONTAINER_IMAGE=$(docker inspect -f '{{.Config.Image}}' $DOCKER_CONTAINER_NAME 2>/dev/null)
IMAGE_CREATED=$(docker inspect -f '{{.Created}}' $DOCKER_CONTAINER_NAME 2>/dev/null)
echo "   容器镜像：$CONTAINER_IMAGE"
echo "   镜像创建时间：$IMAGE_CREATED"

# 获取本地最新镜像
LOCAL_IMAGE=$(docker images --format "{{.Repository}}:{{.Tag}}" | grep $PROJECT_NAME | head -1)
LOCAL_IMAGE_CREATED=$(docker inspect -f '{{.Created}}' $LOCAL_IMAGE 2>/dev/null)
echo "   本地最新镜像：$LOCAL_IMAGE"
echo "   本地镜像创建时间：$LOCAL_IMAGE_CREATED"

# 比较镜像创建时间
if [ "$IMAGE_CREATED" = "$LOCAL_IMAGE_CREATED" ]; then
    echo "✅ 容器使用的是最新镜像版本"
else
    # 检查时间差（300秒=5分钟）
    IMAGE_TIMESTAMP=$(date -d "$IMAGE_CREATED" +%s 2>/dev/null || echo "0")
    LOCAL_TIMESTAMP=$(date -d "$LOCAL_IMAGE_CREATED" +%s 2>/dev/null || echo "0")
    TIME_DIFF=$((LOCAL_TIMESTAMP - IMAGE_TIMESTAMP))

    if [ $TIME_DIFF -lt 0 ]; then
        TIME_DIFF=$((-TIME_DIFF))
    fi

    if [ $TIME_DIFF -le 300 ]; then
        echo "✅ 容器使用的是最新镜像版本（时间差：${TIME_DIFF}秒）"
    else
        echo "⚠️  警告：容器可能未使用最新镜像！"
        echo "   容器镜像创建时间：$IMAGE_CREATED"
        echo "   最新镜像创建时间：$LOCAL_IMAGE_CREATED"
        echo "   时间差：${TIME_DIFF}秒"
        exit 1
    fi
fi

# 3. 验证容器健康状态
echo ""
echo "3️⃣  验证容器健康状态..."
HEALTH_STATUS=$(docker inspect -f '{{.State.Health.Status}}' $DOCKER_CONTAINER_NAME 2>/dev/null || echo "无健康检查")
if [ "$HEALTH_STATUS" = "healthy" ] || [ "$HEALTH_STATUS" = "无健康检查" ]; then
    echo "✅ 容器健康状态：$HEALTH_STATUS"
else
    echo "⚠️  容器健康状态：$HEALTH_STATUS"
fi

# 4. 验证容器日志（检查是否有启动错误）
echo ""
echo "4️⃣  验证容器日志（检查启动错误）..."
ERROR_COUNT=$(docker logs --tail 100 $DOCKER_CONTAINER_NAME 2>&1 | grep -i "error\|exception\|fatal" | wc -l)
if [ "$ERROR_COUNT" -gt 0 ]; then
    echo "⚠️  发现 $ERROR_COUNT 个错误日志，请检查："
    docker logs --tail 20 $DOCKER_CONTAINER_NAME
    exit 1
else
    echo "✅ 未发现明显错误日志"
fi

# 5. 验证容器资源使用
echo ""
echo "5️⃣  验证容器资源使用..."
docker stats --no-stream --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}" $DOCKER_CONTAINER_NAME

# 6. 最终验证总结
echo ""
echo "============================================="
echo "📋 验证总结"
echo "============================================="
echo "容器名称：$DOCKER_CONTAINER_NAME"
echo "运行状态：$CONTAINER_STATUS"
echo "镜像版本：$CONTAINER_IMAGE"
echo "健康状态：$HEALTH_STATUS"
echo "错误日志：$ERROR_COUNT 个"
echo "============================================="

if [ "$ERROR_COUNT" -eq 0 ] && [ -n "$CONTAINER_STATUS" ]; then
    echo "✅ 验证通过！容器已成功更新到最新版本！"
    exit 0
else
    echo "❌ 验证失败！请检查上述错误！"
    exit 1
fi
VERIFYEOF

chmod +x /tmp/verify_deployment.sh

# 上传验证脚本到服务器
scp -o StrictHostKeyChecking=no \
    -o UserKnownHostsFile=/dev/null \
    /tmp/verify_deployment.sh \
    "$SERVER_USER@$SERVER_IP:/tmp/verify_deployment.sh"

# 在服务器上执行验证
ssh -o StrictHostKeyChecking=no \
    -o UserKnownHostsFile=/dev/null \
    "$SERVER_USER@$SERVER_IP" \
    "bash /tmp/verify_deployment.sh '$DOCKER_CONTAINER_NAME' '$PROJECT_NAME'"

# 检查验证结果
if [ $? -eq 0 ]; then
    echo "============================================="
    echo "🎉 一键部署完成！验证通过！"
    echo "============================================="
else
    echo "============================================="
    echo "⚠️  部署完成但验证失败！请检查上述错误！"
    echo "============================================="
    echo ""
    echo "🔧 建议执行以下命令重新部署："
    echo ""
    echo "ssh root@$SERVER_IP << 'EOF'"
    echo "cd /root/$PROJECT_NAME"
    echo "docker-compose down"
    echo "docker rmi $DOCKER_IMAGE_NAME --force"
    echo "docker-compose build --no-cache"
    echo "docker-compose up -d"
    echo "EOF"
    echo ""
    exit 1
fi

# 步骤5：清理临时文件
echo ""
echo "📤 步骤 5/5: 清理临时文件..."
rm -f /tmp/verify_deployment.sh
ssh -o StrictHostKeyChecking=no \
    -o UserKnownHostsFile=/dev/null \
    "$SERVER_USER@$SERVER_IP" \
    "rm -f /tmp/verify_deployment.sh /root/$DEPLOY_PACKAGE_NAME"
echo "✅ 临时文件已清理"

echo ""
echo "============================================="
echo "🎉 部署全部完成！"
echo "============================================="
```

**执行权限:**

```bash
chmod +x one_click_deploy.sh
```

#### 3.1.5 步骤5: 执行一键部署

```bash
# 执行一键部署
./one_click_deploy.sh
```

---

## 4. 部署验证

### 4.1 验证清单

部署完成后,必须验证以下项目:

#### 4.1.1 容器状态验证

```bash
# 检查容器运行状态
ssh trading-prod "docker ps -f name=trading-system"

# 检查容器镜像版本
ssh trading-prod "docker inspect -f '{{.Config.Image}}' trading-system"

# 检查镜像创建时间
ssh trading-prod "docker inspect -f '{{.Created}}' trading-system"
```

#### 4.1.2 服务健康验证

```bash
# 检查容器健康状态
ssh trading-prod "docker inspect -f '{{.State.Health.Status}}' trading-system"

# 检查容器日志
ssh trading-prod "docker logs --tail 50 trading-system"

# 检查错误日志
ssh trading-prod "docker logs --tail 100 trading-system | grep -i error"
```

#### 4.1.3 数据库连接验证

```bash
# 检查数据库连接
ssh trading-prod "docker exec trading-system python -c 'from shared.database import DatabaseService; db = DatabaseService(); print(db.get_klines(\"BTCUSDT\", \"1h\", limit=1))'"

# 检查数据库表
ssh trading-prod "docker exec -it postgres-db psql -U trading_user -d trading_platform -c '\dt trading.*'"
```

#### 4.1.4 API连接验证

```bash
# 检查Binance API连接
ssh trading-prod "docker exec trading-system python -c 'from shared.binance_api import BinanceAPI; api = BinanceAPI(); print(api.get_server_time())'"

# 检查K线服务连接
ssh trading-prod "docker exec trading-system python -c 'from shared.kline_service import KlineService; ks = KlineService(); print(ks.get_klines(\"BTCUSDT\", \"1h\", limit=1))'"
```

#### 4.1.5 通知服务验证

```bash
# 发送测试通知
ssh trading-prod "docker exec trading-system python -c 'from shared.notification import NotificationService; ns = NotificationService(); print(ns.send_message(\"测试通知\", level=\"info\"))'"
```

### 4.2 功能测试

#### 4.2.1 策略启动测试

```bash
# 检查策略是否正常启动
ssh trading-prod "docker logs trading-system | grep -i '策略启动'"

# 检查策略配置是否加载
ssh trading-prod "docker logs trading-system | grep -i '配置加载'"
```

#### 4.2.2 K线数据获取测试

```bash
# 测试K线数据获取
ssh trading-prod "docker exec trading-system python -c '
from shared.kline_service import KlineService
from shared.binance_api import BinanceAPI
from shared.database import DatabaseService

api = BinanceAPI()
db = DatabaseService()
ks = KlineService(db=db, binance_api=api)

klines = ks.get_klines(\"BTCUSDT\", \"1h\", limit=10)
print(f\"获取到 {len(klines)} 条K线数据\")
'"
```

#### 4.2.3 评分引擎测试

```bash
# 测试评分引擎
ssh trading-prod "docker exec trading-system python -c '
from strategies.btc_eth.strategy import BTCETHStrategy
from shared.kline_service import KlineService
from shared.binance_api import BinanceAPI
from shared.database import DatabaseService
import yaml

# 加载配置
with open(\"/app/strategy/config.yaml\", \"r\") as f:
    config = yaml.safe_load(f)

# 初始化服务
api = BinanceAPI()
db = DatabaseService()
ks = KlineService(db=db, binance_api=api)

# 获取指标
indicators = ks.get_indicators(\"BTCUSDT\", \"1h\")
print(f\"获取到指标数据：{indicators is not None}\")
'"
```

### 4.3 性能测试

#### 4.3.1 启动时间测试

```bash
# 测试策略启动时间
ssh trading-prod "time docker restart trading-system"
```

**目标**: 启动时间 < 30秒

#### 4.3.2 API响应时间测试

```bash
# 测试API响应时间
ssh trading-prod "docker exec trading-system python -c '
import time
from shared.binance_api import BinanceAPI

api = BinanceAPI()

start = time.time()
api.get_server_time()
end = time.time()

print(f\"API响应时间：{(end - start) * 1000:.2f}ms\")
'"
```

**目标**: API响应时间 < 100ms

#### 4.3.3 数据库查询性能测试

```bash
# 测试数据库查询性能
ssh trading-prod "docker exec trading-system python -c '
import time
from shared.database import DatabaseService

db = DatabaseService()

start = time.time()
klines = db.get_klines(\"BTCUSDT\", \"1h\", limit=100)
end = time.time()

print(f\"查询到 {len(klines)} 条数据，耗时：{(end - start) * 1000:.2f}ms\")
'"
```

**目标**: 数据库查询时间 < 100ms

---

## 5. 故障排查

### 5.1 常见问题

#### 5.1.1 SSH密钥配置问题

**症状**: 配置了SSH密钥,但登录时仍然提示输入密码

**解决方案**:

```bash
# 1. 检查/root目录的所有权和权限
ssh trading-prod "ls -ld /root"

# 如果所有者不是root:root,修复它
ssh trading-prod "chown root:root /root && chmod 700 /root"

# 2. 检查.ssh目录权限
ssh trading-prod "chmod 700 ~/.ssh && chmod 600 ~/.ssh/authorized_keys"

# 3. 验证公钥指纹匹配
ssh-keygen -lf /Users/yl/vscode/inspection_automation/docs/only.pem
ssh trading-prod "ssh-keygen -lf ~/.ssh/authorized_keys"

# 4. 使用详细模式调试
ssh -vvv -o StrictHostKeyChecking=no -o IdentitiesOnly=yes \
    -i /Users/yl/vscode/inspection_automation/docs/only.pem \
    root@43.156.242.184 "echo test"
```

#### 5.1.2 文件打包遗漏问题

**症状**: 部署后发现某些文件未上传到服务器

**解决方案**:

```bash
# 1. 检查压缩包内容
tar -tzf deployment_package.tar.gz | head -50

# 2. 解压压缩包到临时目录
mkdir -p /tmp/verify_package
tar -xzf deployment_package.tar.gz -C /tmp/verify_package

# 3. 统计文件数量
echo "本地文件数量："
find . -type f ! -path './.git/*' ! -path './logs/*' | wc -l

echo "压缩包文件数量："
find /tmp/verify_package -type f | wc -l

# 4. 检查特定文件是否存在
tar -tzf deployment_package.tar.gz | grep "missing_file" || echo "文件不在压缩包中"

# 5. 清理临时文件
rm -rf /tmp/verify_package
```

#### 5.1.3 容器未使用最新代码问题

**症状**: 部署完成后,容器内运行的还是旧代码

**解决方案**:

```bash
# 1. 检查容器使用的镜像
ssh trading-prod "docker inspect -f '{{.Config.Image}}' trading-system"

# 2. 检查镜像创建时间
ssh trading-prod "docker inspect -f '{{.Created}}' trading-system"

# 3. 强制重新部署
ssh trading-prod << 'EOF'
cd /root/Binance_quantitative_trading
docker-compose down
docker images | grep trading-system | awk '{print $3}' | xargs docker rmi --force
docker-compose build --no-cache
docker-compose up -d
EOF

# 4. 验证容器内代码
ssh trading-prod "docker exec trading-system ls -la /app"
ssh trading-prod "docker exec trading-system stat /app/strategies/btc_eth/main.py"
```

#### 5.1.4 数据库连接失败问题

**症状**: 应用无法连接到PostgreSQL数据库

**解决方案**:

```bash
# 1. 检查PostgreSQL容器状态
ssh trading-prod "docker ps -f name=postgres-db"

# 2. 检查网络连接
ssh trading-prod "docker network ls"
ssh trading-prod "docker network inspect trading-network"

# 3. 验证数据库配置
ssh trading-prod "cat /root/Binance_quantitative_trading/.env | grep DATABASE_URL"

# 4. 测试连接
ssh trading-prod "docker exec -it postgres-db psql -U trading_user -d trading_platform -c 'SELECT 1;'"
```

#### 5.1.5 API调用失败问题

**症状**: Binance API调用失败

**解决方案**:

```bash
# 1. 检查API密钥配置
ssh trading-prod "docker exec trading-system env | grep BINANCE"

# 2. 测试API连接
ssh trading-prod "docker exec trading-system python -c '
from shared.binance_api import BinanceAPI
api = BinanceAPI()
try:
    time = api.get_server_time()
    print(f\"API连接成功，服务器时间：{time}\")
except Exception as e:
    print(f\"API连接失败：{e}\")
'"

# 3. 检查网络连接
ssh trading-prod "docker exec trading-system curl -I https://papi.binance.com"
```

### 5.2 日志查看

#### 5.2.1 容器日志

```bash
# 查看实时日志
ssh trading-prod "docker logs -f trading-system"

# 查看最近100行日志
ssh trading-prod "docker logs --tail 100 trading-system"

# 查看错误日志
ssh trading-prod "docker logs trading-system | grep -i error"

# 查看特定时间段的日志
ssh trading-prod "docker logs --since 2026-05-05T10:00:00 trading-system"
```

#### 5.2.2 应用日志

```bash
# 查看策略日志
ssh trading-prod "tail -f /root/Binance_quantitative_trading/logs/btc_eth_strategy.log"

# 查看系统日志
ssh trading-prod "tail -f /var/log/syslog | grep docker"
```

### 5.3 紧急回滚

**回滚触发条件**:
- 策略运行异常,无法正常交易
- 数据丢失或损坏
- 性能严重下降,影响交易

**回滚步骤**:

```bash
# 1. 停止新系统
ssh trading-prod "docker-compose -f /root/Binance_quantitative_trading/docker-compose.yml down"

# 2. 恢复备份(如果有)
ssh trading-prod "cd /root && tar -xzf /root/backup/trading_backup_YYYYMMDD.tar.gz"

# 3. 启动旧系统(如果有)
# ssh trading-prod "cd /root/old_project && docker-compose up -d"

# 4. 验证系统
ssh trading-prod "docker ps"
ssh trading-prod "docker logs trading-system"
```

---

## 6. 日常运维

### 6.1 监控命令

```bash
# 查看容器状态
ssh trading-prod "docker ps -f name=trading-system"

# 查看容器资源使用
ssh trading-prod "docker stats trading-system"

# 查看容器日志
ssh trading-prod "docker logs --tail 50 trading-system"

# 查看数据库连接数
ssh trading-prod "docker exec -it postgres-db psql -U trading_user -d trading_platform -c 'SELECT count(*) FROM pg_stat_activity;'"
```

### 6.2 维护命令

```bash
# 重启容器
ssh trading-prod "docker restart trading-system"

# 清理日志
ssh trading-prod "docker exec trading-system rm -rf /app/logs/*.log"

# 清理Docker缓存
ssh trading-prod "docker system prune -f"

# 备份数据库
ssh trading-prod "docker exec postgres-db pg_dump -U trading_user trading_platform > /root/backup/trading_db_$(date +%Y%m%d).sql"
```

### 6.3 更新部署

```bash
# 拉取最新代码
cd /Users/yl/vscode/Binance_quantitative_trading
git pull

# 执行一键部署
./one_click_deploy.sh
```

---

## 7. 安全建议

### 7.1 密钥管理

- ✅ 使用SSH密钥认证,避免密码登录
- ✅ 定期更换API密钥
- ✅ 敏感信息存储在环境变量中
- ✅ 不要将密钥提交到Git仓库

### 7.2 访问控制

- ✅ 限制SSH访问IP
- ✅ 使用防火墙规则
- ✅ 最小权限原则
- ✅ 定期审计访问日志

### 7.3 数据安全

- ✅ 定期备份数据库
- ✅ 加密敏感数据
- ✅ 日志脱敏处理
- ✅ 监控异常访问

---

## 8. 附录

### 8.1 部署检查清单

**部署前检查**:
- [ ] SSH密钥配置完成
- [ ] 服务器资源充足
- [ ] 数据库服务正常
- [ ] 通用服务正常
- [ ] 配置文件准备完成

**部署中检查**:
- [ ] 打包成功
- [ ] 上传成功
- [ ] 容器构建成功
- [ ] 容器启动成功

**部署后检查**:
- [ ] 容器运行正常
- [ ] 数据库连接正常
- [ ] API连接正常
- [ ] 通知服务正常
- [ ] 策略运行正常

### 8.2 快速参考命令

```bash
# 一键部署
./one_click_deploy.sh

# 查看容器状态
ssh trading-prod "docker ps -f name=trading-system"

# 查看容器日志
ssh trading-prod "docker logs -f trading-system"

# 重启容器
ssh trading-prod "docker restart trading-system"

# 进入容器
ssh trading-prod "docker exec -it trading-system /bin/bash"
```

---

**文档结束**
