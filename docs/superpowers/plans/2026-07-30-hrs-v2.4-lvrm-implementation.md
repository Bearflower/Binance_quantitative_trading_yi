# HRS V2.4 LV-RM 模块实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use subagent-driven-development or executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 HRS V2.4 低波动反转模块（LV-RM），覆盖低波动震荡市中的均值回归机会。

**Architecture:** 在现有三轨框架（EMM → 半EMM → 标准模式）后新增第四道检查——LV-RM，从候选池落选币种中筛选低波动标的，使用布林带触轨 + RSI + 4h趋势过滤入场，独立止损止盈。

**Tech Stack:** Python 3.10+, asyncio, Binance Futures API, 1h/4h K线

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `strategies/hrs/config.yaml` | 修改 | 新增 LV-RM 配置段，更新版本号 |
| `strategies/hrs/scoring_engine.py` | 修改 | 新增 `lv_rm_entry_mode` 常量、LV 技术评分方法、`ScoringResult` 字段 |
| `strategies/hrs/strategy.py` | 修改 | 新增 `_check_lv_rm_entry()` 方法，修改 `analyze()` 和 `_execute_cycle()` |
| `strategies/hrs/executor.py` | 修改 | 新增 `entry_mode` 参数，LV-RM 独立止损止盈计算 |
| `strategies/hrs/candidate_pool.py` | 修改 | 新增 `get_low_volatility_candidates()` 方法 |

---

### Task 1: 更新 config.yaml 配置

**Files:**
- Modify: `strategies/hrs/config.yaml`

- [ ] **Step 1: 更新版本号和描述**

```yaml
  version: "2.4.0"
  description: "混合反转策略 V2.4 - 三轨并行（标准模式 + EMM + LV-RM），覆盖低波动震荡市"
```

- [ ] **Step 2: 新增 LV-RM 配置段**

在 `candidate_pool` 段后新增：

```yaml
# ============================================================
# V2.4 低波动反转模块（LV-RM）配置
# ============================================================
lv_rm:
  enabled: true                       # 是否启用 LV-RM 模块
  # 扫描范围：从候选池落选币种中筛选
  scan:
    max_price_change_24h: 0.05        # 24h涨跌幅 < 5%（低波动筛选阈值）
    api_concurrency_limit: 10         # LV-RM 检查并发数限制
  # 入场条件配置
  entry:
    # 布林带配置（1h K线）
    bollinger_period: 20              # 布林带周期
    bollinger_std: 2.0                # 布林带标准差倍数
    # RSI 配置（1h K线）
    rsi_period: 14                    # RSI 周期
    rsi_overbought: 70                # 超买阈值
    rsi_oversold: 30                  # 超卖阈值
    # 资金费率阈值
    funding_rate_short: 0.30          # 做空：费率 ≥ 30% 年化
    funding_rate_long: -0.10          # 做多：费率 ≤ -10% 年化
    # 4h EMA20 趋势过滤（一票否决）
    trend_filter:
      enabled: true                   # 是否启用趋势过滤
      ema_period: 20                  # 4h EMA 周期
      long:
        min_price: 1.0                # 做多：价格 ≥ EMA20 × 1.0（必须 > EMA20）
        max_deviation: 0.97           # 做多：价格 ≥ EMA20 × 0.97（偏离不超过 -3%）
      short:
        max_price: 1.0                # 做空：价格 ≤ EMA20 × 1.0（必须 < EMA20）
        max_deviation: 1.03           # 做空：价格 ≤ EMA20 × 1.03（偏离不超过 +3%）
  # 评分配置
  scoring:
    # 权重（与标准模式共用 weights，但技术分只包含布林带+RSI）
    # 权重：contract=0.25, technical=0.45, sentiment=0.30
    entry_threshold: 6.0              # LV总分 ≥ 6.0
    technical:
      min_total_score: 4.0            # LV技术分 ≥ 4.0
      # 布林带触轨评分
      bollinger_breakout:
        base_score: 2.0               # 触轨基础分
        cap_score: 4.0                # 封顶分
        multiplier: 2.0               # 突破幅度系数
      # RSI 评分
      rsi:
        score: 3.0                    # RSI达标即满分
  # 止损止盈配置（独立，均值回归风格）
  stop_loss:
    atr_multiplier: 1.5               # ATR 止损倍数
    emergency_percent: 0.01           # 紧急止损 1%
    min_absolute_percent: 0.03        # 最小绝对止损 3%
  take_profit:
    target1_atr_multiplier: 1.0       # 第一目标 1.0×ATR
    target1_close_percent: 0.30       # 平30%
    target2_atr_multiplier: 2.0       # 第二目标 2.0×ATR
    target2_close_percent: 0.40       # 平40%
    trailing_stop_atr_multiplier: 1.0 # 移动止盈回撤 1.0×ATR
  time_stop:
    max_holding_hours: 48             # 时间止损 48小时
  # 单日开仓限制（独立）
  daily_limit:
    max_positions: 2                  # 每日最多2个币种
```

- [ ] **Step 3: 验证 YAML 格式（可选）**

Run: `python -c "import yaml; yaml.safe_load(open('strategies/hrs/config.yaml'))"`

---

### Task 2: 更新 ScoringEngine 评分引擎

**Files:**
- Modify: `strategies/hrs/scoring_engine.py`

- [ ] **Step 1: 在 ScoringResult 中新增字段**

在 `ScoringResult` 中新增 `bb_position` 和 `rsi_value` 字段：

```python
    bb_position: Optional[float] = None        # V2.4 LV-RM: 入场时价格在布林带中的位置
    rsi_value: Optional[float] = None          # V2.4 LV-RM: 入场时的 RSI 值
```

在 `to_dict()` 中添加这两个字段：

```python
            "bb_position": self.bb_position,
            "rsi_value": self.rsi_value,
```

- [ ] **Step 2: 新增 LV-RM 评分方法**

在 `ScoringEngine` 类中新增 `score_lv_rm` 方法：

```python
    async def score_lv_rm(
        self,
        symbol: str,
        direction: str,
        oi_market_cap_ratio: float,
        funding_rate: float,
        has_market_cap: bool,
        klines_1h: List[Dict],
        klines_4h: List[Dict],
    ) -> ScoringResult:
        """
        V2.4: LV-RM 低波动反转评分

        与标准评分区别：
        - 技术分仅包含布林带触轨和RSI，不包含形态检测
        - 增加4h趋势过滤（一票否决）
        - 独立评分门槛

        Args:
            symbol: 交易对
            direction: 'short' 或 'long'
            oi_market_cap_ratio: OI/市值比
            funding_rate: 年化资金费率
            has_market_cap: 是否有市值数据
            klines_1h: 1h K线数据（用于计算布林带和RSI）
            klines_4h: 4h K线数据（用于趋势过滤）

        Returns:
            ScoringResult 对象
        """
        lv_rm_config = self.config.get("lv_rm", {})
        entry_config = lv_rm_config.get("entry", {})
        scoring_config = lv_rm_config.get("scoring", {})
        tech_config = scoring_config.get("technical", {})
        bb_config = tech_config.get("bollinger_breakout", {})
        rsi_config = tech_config.get("rsi", {})

        # 1. 合约分（与标准模式共用）
        contract_score = self._calc_contract_score(direction, oi_market_cap_ratio, has_market_cap)

        # 2. 计算布林带
        bb_period = entry_config.get("bollinger_period", 20)
        bb_std = entry_config.get("bollinger_std", 2.0)
        upper, middle, lower = self._calc_bollinger_bands(klines_1h, bb_period, bb_std)
        current_close = float(klines_1h[-1].get("close", 0))
        bb_width = upper - lower if upper - lower > 0 else 1e-10

        # 3. 计算 RSI
        rsi_period = entry_config.get("rsi_period", 14)
        rsi = self._calc_rsi(klines_1h, rsi_period)

        # 4. 计算 4h EMA20
        ema_config = entry_config.get("trend_filter", {}).get("ema_period", 20)
        ema_4h = self._calc_ema(klines_4h, ema_config)
        current_close_4h = float(klines_4h[-1].get("close", 0)) if klines_4h else current_close

        # 5. 执行4h趋势过滤（一票否决）
        trend_filter_config = entry_config.get("trend_filter", {})
        trend_ok, trend_reason = self._check_lv_rm_trend_filter(
            direction=direction,
            current_price_4h=current_close_4h,
            ema_4h=ema_4h,
            config=trend_filter_config,
        )
        if not trend_ok:
            return ScoringResult(
                symbol=symbol,
                direction=direction,
                total_score=0.0,
                contract_score=contract_score,
                technical_score=0.0,
                sentiment_score=0.0,
                veto=True,
                veto_reason=trend_reason,
                entry_mode="lv_rm",
                details={"trend_filter": trend_reason},
            )

        # 6. 检查入场条件（1h级别）
        # 做空：价格≥布林上轨，RSI≥70，费率≥30%
        # 做多：价格≤布林下轨，RSI≤30，费率≤-10%
        rsi_ok = (direction == "short" and rsi >= rsi_config.get("score_for_overbought", 70)) or \
                 (direction == "long" and rsi <= rsi_config.get("score_for_oversold", 30))
        if not rsi_ok:
            return ScoringResult(
                symbol=symbol,
                direction=direction,
                total_score=0.0,
                contract_score=contract_score,
                technical_score=0.0,
                sentiment_score=0.0,
                veto=True,
                veto_reason=f"RSI({rsi:.1f})未达标",
                entry_mode="lv_rm",
                details={"rsi": rsi},
            )

        # 价格位置检查
        if direction == "short":
            price_ok = current_close >= upper  # 价格 ≥ 布林上轨
            bb_breakout = (current_close - upper) / bb_width if bb_width > 0 else 0
        else:
            price_ok = current_close <= lower  # 价格 ≤ 布林下轨
            bb_breakout = (lower - current_close) / bb_width if bb_width > 0 else 0
        if not price_ok:
            return ScoringResult(
                symbol=symbol,
                direction=direction,
                total_score=0.0,
                contract_score=contract_score,
                technical_score=0.0,
                sentiment_score=0.0,
                veto=True,
                veto_reason="价格未触及布林带轨道",
                entry_mode="lv_rm",
                details={"current_close": current_close, "upper": upper, "lower": lower},
            )

        # K线确认（止跌/滞涨）
        kline_confirm_ok = self._check_lv_rm_kline_confirm(direction, klines_1h)
        if not kline_confirm_ok:
            return ScoringResult(
                symbol=symbol,
                direction=direction,
                total_score=0.0,
                contract_score=contract_score,
                technical_score=0.0,
                sentiment_score=0.0,
                veto=True,
                veto_reason="K线确认失败",
                entry_mode="lv_rm",
                details={},
            )

        # 7. 计算 LV 技术分
        bb_base_score = bb_config.get("base_score", 2.0)
        bb_cap_score = bb_config.get("cap_score", 4.0)
        bb_multiplier = bb_config.get("multiplier", 2.0)
        bollinger_score = min(bb_base_score + min(bb_breakout * bb_multiplier, bb_cap_score - bb_base_score), bb_cap_score)

        rsi_score = rsi_config.get("score", 3.0) if rsi_ok else 0.0

        lv_technical_score = min(bollinger_score + rsi_score, 7.0)  # 封顶7分

        # 8. 情绪分（与标准模式共用）
        sentiment_score = self._calc_sentiment_score(direction, funding_rate * 100)  # 转换为百分比

        # 9. 总分
        total_score = (
            contract_score * self.contract_weight
            + lv_technical_score * self.technical_weight
            + sentiment_score * self.sentiment_weight
        )

        lv_entry_threshold = scoring_config.get("entry_threshold", 6.0)
        lv_min_technical = tech_config.get("min_total_score", 4.0)

        should_entry = total_score >= lv_entry_threshold and lv_technical_score >= lv_min_technical

        return ScoringResult(
            symbol=symbol,
            direction=direction,
            total_score=total_score,
            contract_score=contract_score,
            technical_score=lv_technical_score,
            sentiment_score=sentiment_score,
            veto=not should_entry,
            veto_reason="" if should_entry else f"LV总分({total_score:.2f})或技术分({lv_technical_score:.1f})不达标",
            entry_mode="lv_rm",
            bb_position=bb_breakout,
            rsi_value=rsi,
            details={
                "bollinger_score": bollinger_score,
                "rsi_score": rsi_score,
                "rsi": rsi,
                "bb_breakout": bb_breakout,
                "trend_ok": trend_ok,
            },
        )
```

- [ ] **Step 3: 新增辅助方法**

在 `ScoringEngine` 中新增以下辅助方法：

```python
    def _calc_bollinger_bands(self, klines: List[Dict], period: int = 20, std: float = 2.0) -> Tuple[float, float, float]:
        """
        计算布林带（上轨、中轨、下轨）

        Args:
            klines: K线数据列表
            period: 周期
            std: 标准差倍数

        Returns:
            (upper, middle, lower) 三元组
        """
        if len(klines) < period:
            return 0.0, 0.0, 0.0

        closes = [float(k.get("close", 0)) for k in klines[-period:]]
        middle = sum(closes) / len(closes)
        variance = sum((c - middle) ** 2 for c in closes) / len(closes)
        std_dev = variance ** 0.5
        upper = middle + std * std_dev
        lower = middle - std * std_dev
        return upper, middle, lower

    def _calc_rsi(self, klines: List[Dict], period: int = 14) -> float:
        """
        计算 RSI

        Args:
            klines: K线数据列表
            period: 周期

        Returns:
            RSI 值（0-100）
        """
        if len(klines) < period + 1:
            return 50.0

        closes = [float(k.get("close", 0)) for k in klines[-(period + 1):]]
        gains = []
        losses = []
        for i in range(1, len(closes)):
            diff = closes[i] - closes[i - 1]
            gains.append(max(diff, 0))
            losses.append(max(-diff, 0))

        avg_gain = sum(gains[-period:]) / period
        avg_loss = sum(losses[-period:]) / period
        if avg_loss == 0:
            return 100.0
        rs = avg_gain / avg_loss
        return 100.0 - (100.0 / (1.0 + rs))

    def _calc_ema(self, klines: List[Dict], period: int = 20) -> float:
        """
        计算 EMA

        Args:
            klines: K线数据列表
            period: 周期

        Returns:
            EMA 值
        """
        if len(klines) < period:
            return float(klines[-1].get("close", 0)) if klines else 0.0

        closes = [float(k.get("close", 0)) for k in klines[-period * 2:]]  # 多取一些数据保证收敛
        multiplier = 2.0 / (period + 1)
        ema = sum(closes[:period]) / period  # SMA 初始值
        for price in closes[period:]:
            ema = (price - ema) * multiplier + ema
        return ema

    def _check_lv_rm_trend_filter(
        self,
        direction: str,
        current_price_4h: float,
        ema_4h: float,
        config: Dict[str, Any],
    ) -> Tuple[bool, str]:
        """
        V2.4: 检查 LV-RM 4h趋势过滤

        - 做多：价格 > EMA20 且 价格 ≥ EMA20 × 0.97
        - 做空：价格 < EMA20 且 价格 ≤ EMA20 × 1.03

        Returns:
            (是否通过, 失败原因)
        """
        if not config.get("enabled", True):
            return True, "趋势过滤未启用"

        if ema_4h <= 0:
            return True, "EMA20数据不足，跳过趋势过滤"

        if direction == "long":
            long_config = config.get("long", {})
            min_price_ratio = long_config.get("min_price", 1.0)
            max_deviation = long_config.get("max_deviation", 0.97)
            # 做多：价格 > EMA20（多头排列）
            if current_price_4h <= ema_4h * min_price_ratio:
                return False, f"4h趋势过滤：价格({current_price_4h:.4f})未超过EMA20({ema_4h:.4f})，不做多"
            # 做多：偏离不超过 -3%
            if current_price_4h < ema_4h * max_deviation:
                return False, f"4h趋势过滤：价格({current_price_4h:.4f})偏离EMA20({ema_4h:.4f})超过-3%，禁止抄底"
        else:  # short
            short_config = config.get("short", {})
            max_price_ratio = short_config.get("max_price", 1.0)
            max_deviation = short_config.get("max_deviation", 1.03)
            # 做空：价格 < EMA20（空头排列）
            if current_price_4h >= ema_4h * max_price_ratio:
                return False, f"4h趋势过滤：价格({current_price_4h:.4f})未低于EMA20({ema_4h:.4f})，不做空"
            # 做空：偏离不超过 +3%
            if current_price_4h > ema_4h * max_deviation:
                return False, f"4h趋势过滤：价格({current_price_4h:.4f})偏离EMA20({ema_4h:.4f})超过+3%，禁止摸顶"

        return True, ""

    def _check_lv_rm_kline_confirm(self, direction: str, klines: List[Dict]) -> bool:
        """
        V2.4: 检查 LV-RM K线确认

        - 做多：收盘价 > 前一根K线最低价（止跌确认）
        - 做空：收盘价 < 前一根K线最高价（滞涨确认）

        Args:
            direction: 'short' 或 'long'
            klines: 1h K线数据

        Returns:
            True: 确认通过；False: 确认失败
        """
        if len(klines) < 2:
            return False

        current_close = float(klines[-1].get("close", 0))
        prev_high = float(klines[-2].get("high", 0))
        prev_low = float(klines[-2].get("low", 0))

        if direction == "long":
            return current_close > prev_low  # 止跌
        else:  # short
            return current_close < prev_high  # 滞涨
```

- [ ] **Step 4: 在 `should_entry` 方法中处理 `lv_rm` 模式**

```python
        # V2.4：LV-RM 使用独立评分门槛
        if score_result.entry_mode == "lv_rm":
            lv_rm_config = self.config.get("lv_rm", {}).get("scoring", {})
            lv_threshold = lv_rm_config.get("entry_threshold", 6.0)
            lv_min_technical = lv_rm_config.get("technical", {}).get("min_total_score", 4.0)
            if score_result.total_score < lv_threshold:
                return False
            if score_result.technical_score < lv_min_technical:
                return False
            return True

        # V2.2：EMM/半EMM/标准模式使用各自入场阈值
        ...
```

- [ ] **Step 5: 运行测试**

Run: `cd /Users/yl/vscode/Binance_quantitative_trading && python -m pytest strategies/hrs/tests/ -v -x 2>&1 | tail -30`

---

### Task 3: 更新 CandidatePool 候选池

**Files:**
- Modify: `strategies/hrs/candidate_pool.py`

- [ ] **Step 1: 跟踪落选币种**

在 `CandidatePool` 类中新增方法，记录所有通过流动性但未进入候选池的币种：

```python
    # V2.4: 落选币种缓存（通过流动性门槛但未满足候选池条件的币种，用于LV-RM扫描）
    _eliminated_symbols: List[str] = []

    def get_eliminated_symbols(self) -> List[str]:
        """
        V2.4: 获取落选币种列表

        返回通过流动性门槛但未进入候选池的所有币种。
        """
        return self._eliminated_symbols

    def get_low_volatility_candidates(self) -> List[str]:
        """
        V2.4: 获取低波动候选币种

        从落选币种中筛选出24h涨跌幅 < 5%的低波动币种。
        用于LV-RM模块扫描。

        Returns:
            低波动币种列表
        """
        lv_rm_config = self.config.get("lv_rm", {})
        scan_config = lv_rm_config.get("scan", {})
        max_price_change = scan_config.get("max_price_change_24h", 0.05)

        low_vol_symbols = []
        for symbol in self._eliminated_symbols:
            price_change = self._ticker_data.get(symbol, {}).get("price_change_percent", 0)
            # price_change_percent 可能是百分比数值（如 3.5 表示 3.5%）或小数（如 0.035）
            if isinstance(price_change, (int, float)):
                change = abs(price_change / 100.0) if price_change > 1 else abs(price_change)
                if change < max_price_change:
                    low_vol_symbols.append(symbol)

        logger.info(
            "LV-RM低波动候选币种",
            total_eliminated=len(self._eliminated_symbols),
            low_volatility_count=len(low_vol_symbols),
        )
        return low_vol_symbols
```

- [ ] **Step 2: 在扫描过程中收集落选币种**

在 `CandidatePool` 的扫描方法中，在筛选完成后记录落选币种：

找到 `_scan_candidates` 或类似方法中筛选候选池的逻辑，在每次筛选后记录：
```python
        # V2.4: 记录落选币种（通过流动性但未通过候选条件的币种，用于LV-RM扫描）
        self._eliminated_symbols = [
            s for s in self._all_qualified_symbols  # 所有通过流动性门槛的币种
            if s not in short_candidates and s not in long_candidates
        ]
```

- [ ] **Step 3: 运行测试**

Run: `cd /Users/yl/vscode/Binance_quantitative_trading && python -m pytest strategies/hrs/tests/ -v -x 2>&1 | tail -30`

---

### Task 4: 更新 Strategy 主策略

**Files:**
- Modify: `strategies/hrs/strategy.py`

- [ ] **Step 1: 在 `_execute_cycle` 中新增 LV-RM 检查**

在现有候选池评分循环之后，新增 LV-RM 检查：

```python
        # ===== V2.4: LV-RM 低波动反转模块检查 =====
        lv_rm_config = self.config.get("lv_rm", {})
        if lv_rm_config.get("enabled", True):
            try:
                # 获取低波动候选币种
                low_vol_symbols = self.candidate_pool.get_low_volatility_candidates()
                if low_vol_symbols:
                    await self._check_lv_rm_entries(low_vol_symbols, short_signals, long_signals)
            except Exception as e:
                logger.error("LV-RM检查失败", error=str(e))
```

插入位置：在 `# 处理双向冲突` 之前，`# 发送周期汇总通知` 之前。

- [ ] **Step 2: 新增 `_check_lv_rm_entries` 方法**

```python
    async def _check_lv_rm_entries(
        self,
        low_vol_symbols: List[str],
        short_signals: List[Dict],
        long_signals: List[Dict],
    ) -> None:
        """
        V2.4: 检查LV-RM低波动反转信号

        对低波动候选币种执行LV-RM评分，检查入场条件。

        Args:
            low_vol_symbols: 低波动币种列表
            short_signals: 做空信号列表（标准模式，将被追加）
            long_signals: 做多信号列表（标准模式，将被追加）
        """
        lv_rm_config = self.config.get("lv_rm", {})
        scan_config = lv_rm_config.get("scan", {})
        concurrency_limit = scan_config.get("api_concurrency_limit", 10)

        logger.info(
            "开始LV-RM低波动反转检查",
            symbol_count=len(low_vol_symbols),
            concurrency_limit=concurrency_limit,
        )

        # 并发检查所有低波动币种
        semaphore = asyncio.Semaphore(concurrency_limit)

        async def check_symbol(symbol: str):
            async with semaphore:
                try:
                    # 已在标准模式中检查过的币种跳过
                    if symbol in self.candidate_pool.get_short_candidates() or \
                       symbol in self.candidate_pool.get_long_candidates():
                        return

                    # 检查熔断
                    if self.risk_manager.is_circuit_breaker_active(symbol):
                        return

                    # 获取K线
                    klines = self._klines_cache.get(symbol, [])
                    if len(klines) < self.min_klines:
                        klines = await self._warmup_klines(symbol)
                        if len(klines) < self.min_klines:
                            return
                        self._klines_cache[symbol] = klines

                    # 合成4h K线
                    klines_4h = self._klines_4h_cache.get(symbol, [])

                    current_close = float(klines[-1].get("close", 0))
                    current_open_time = klines[-1].get("open_time", 0)
                    current_close_time = current_open_time + KLINE_INTERVAL_MS.get(self.kline_interval, 3600000)

                    # 获取OI和资金费率
                    oi_usd = await self.market_data.get_oi_usd(symbol)
                    funding_rate = await self.market_data.get_funding_rate(symbol, at_time=current_close_time)
                    volume_24h = await self.market_data.get_24h_volume(symbol)
                    market_cap = await self.market_data.get_market_cap(symbol, oi_usd, volume_24h)
                    has_market_cap = market_cap > 0
                    oi_market_cap_ratio = oi_usd / market_cap if has_market_cap else 0.0

                    # LV-RM 评分
                    score = await self.scoring_engine.score_lv_rm(
                        symbol=symbol,
                        direction="short",
                        oi_market_cap_ratio=oi_market_cap_ratio,
                        funding_rate=funding_rate,
                        has_market_cap=has_market_cap,
                        klines_1h=klines,
                        klines_4h=klines_4h,
                    )
                    should_entry_short = self.scoring_engine.should_entry(score)

                    score_long = await self.scoring_engine.score_lv_rm(
                        symbol=symbol,
                        direction="long",
                        oi_market_cap_ratio=oi_market_cap_ratio,
                        funding_rate=funding_rate,
                        has_market_cap=has_market_cap,
                        klines_1h=klines,
                        klines_4h=klines_4h,
                    )
                    should_entry_long = self.scoring_engine.should_entry(score_long)

                    if should_entry_short:
                        self._record_signal_timestamp(symbol, "short")
                        short_signals.append({
                            "symbol": symbol,
                            "direction": "short",
                            "score": score.total_score,
                            "technical_score": score.technical_score,
                            "sentiment_score": score.sentiment_score,
                            "current_price": current_close,
                            "klines": klines,
                            "entry_mode": "lv_rm",
                        })
                    else:
                        self._clear_signal_timestamp(symbol, "short")

                    if should_entry_long:
                        self._record_signal_timestamp(symbol, "long")
                        long_signals.append({
                            "symbol": symbol,
                            "direction": "long",
                            "score": score_long.total_score,
                            "technical_score": score_long.technical_score,
                            "sentiment_score": score_long.sentiment_score,
                            "current_price": current_close,
                            "klines": klines,
                            "entry_mode": "lv_rm",
                        })
                    else:
                        self._clear_signal_timestamp(symbol, "long")

                except Exception as e:
                    logger.error("LV-RM分析失败", symbol=symbol, error=str(e))

        await asyncio.gather(*[check_symbol(s) for s in low_vol_symbols])
```

- [ ] **Step 3: 在 `_log_signal_to_db` 中处理 `entry_mode`**

确保信号日志记录 `entry_mode` 字段。

- [ ] **Step 4: 运行测试**

Run: `cd /Users/yl/vscode/Binance_quantitative_trading && python -m pytest strategies/hrs/tests/ -v -x 2>&1 | tail -30`

---

### Task 5: 更新 Executor 交易执行器

**Files:**
- Modify: `strategies/hrs/executor.py`

- [ ] **Step 1: 新增 `entry_mode` 参数支持**

在 `TradingExecutor.__init__` 中新增 LV-RM 配置读取：

```python
        # V2.4: LV-RM 独立止损止盈配置
        lv_rm_config = config.get("lv_rm", {})
        lv_sl_config = lv_rm_config.get("stop_loss", {})
        lv_tp_config = lv_rm_config.get("take_profit", {})
        lv_time_config = lv_rm_config.get("time_stop", {})
        self.lv_rm_atr_multiplier = lv_sl_config.get("atr_multiplier", 1.5)
        self.lv_rm_emergency_percent = lv_sl_config.get("emergency_percent", 0.01)
        self.lv_rm_min_absolute_percent = lv_sl_config.get("min_absolute_percent", 0.03)
        self.lv_rm_target1_atr_multiplier = lv_tp_config.get("target1_atr_multiplier", 1.0)
        self.lv_rm_target1_close_percent = lv_tp_config.get("target1_close_percent", 0.30)
        self.lv_rm_target2_atr_multiplier = lv_tp_config.get("target2_atr_multiplier", 2.0)
        self.lv_rm_target2_close_percent = lv_tp_config.get("target2_close_percent", 0.40)
        self.lv_rm_trailing_atr_multiplier = lv_tp_config.get("trailing_stop_atr_multiplier", 1.0)
        self.lv_rm_max_holding_hours = lv_time_config.get("max_holding_hours", 48)
```

- [ ] **Step 2: 修改止损计算使用 `entry_mode`**

找到 `calculate_short_stop_loss` 和 `calculate_long_stop_loss` 方法，或创建新的带 `entry_mode` 的版本：

```python
    async def place_entry_order(
        self,
        symbol: str,
        direction: str,
        entry_price: Decimal,
        quantity: Decimal,
        atr: float,
        klines: List[Dict],
        entry_mode: str = "standard",  # V2.4: 新增参数
    ) -> Optional[Dict]:
        """
        开仓并设置止损止盈

        Args:
            symbol: 交易对
            direction: 方向
            entry_price: 开仓价
            quantity: 数量
            atr: ATR值
            klines: K线数据
            entry_mode: 入场模式 ("standard", "emm", "lv_rm")

        Returns:
            订单结果
        """
        # 根据入场模式选择止损止盈参数
        if entry_mode == "lv_rm":
            atr_multiplier = self.lv_rm_atr_multiplier
            emergency_percent = self.lv_rm_emergency_percent
            min_absolute_percent = self.lv_rm_min_absolute_percent
            target1_atr_multiplier = self.lv_rm_target1_atr_multiplier
            target1_close_percent = self.lv_rm_target1_close_percent
            target2_atr_multiplier = self.lv_rm_target2_atr_multiplier
            target2_close_percent = self.lv_rm_target2_close_percent
            trailing_atr_multiplier = self.lv_rm_trailing_atr_multiplier
            max_holding_hours = self.lv_rm_max_holding_hours
        else:
            atr_multiplier = self.atr_multiplier
            emergency_percent = self.emergency_percent
            min_absolute_percent = self.min_absolute_percent
            target1_atr_multiplier = self.target1_atr_multiplier
            target1_close_percent = self.target1_close_percent
            target2_atr_multiplier = self.target2_atr_multiplier
            target2_close_percent = self.target2_close_percent
            trailing_atr_multiplier = self.trailing_atr_multiplier
            max_holding_hours = self.max_holding_hours

        # 现有的开仓逻辑，使用上述参数...
        # （以下是现有代码，只需替换硬编码的参数引用）
```

- [ ] **Step 3: 修改 `_execute_entry` 方法签名**

找到 `_execute_entry` 方法，将 `entry_mode` 参数传递给 `place_entry_order`。

- [ ] **Step 4: 运行测试**

Run: `cd /Users/yl/vscode/Binance_quantitative_trading && python -m pytest strategies/hrs/tests/ -v -x 2>&1 | tail -30`

---

### Task 6: 更新 strategy.py 中的入场执行

**Files:**
- Modify: `strategies/hrs/strategy.py`

- [ ] **Step 1: 传递 `entry_mode` 给执行器**

找到 `_execute_entry` 或类似方法，修改为传递 `entry_mode`：

```python
    async def _execute_entry(self, signal: Dict[str, Any]) -> None:
        """执行入场"""
        symbol = signal["symbol"]
        direction = signal["direction"]
        entry_mode = signal.get("entry_mode", "standard")  # V2.4: 获取入场模式

        # ... 现有逻辑 ...

        # 调用执行器
        await self.trading_executor.place_entry_order(
            symbol=symbol,
            direction=direction,
            entry_price=Decimal(str(entry_price)),
            quantity=Decimal(str(quantity)),
            atr=atr,
            klines=klines,
            entry_mode=entry_mode,  # V2.4: 传入入场模式
        )
```

- [ ] **Step 2: 运行测试**

Run: `cd /Users/yl/vscode/Binance_quantitative_trading && python -m pytest strategies/hrs/tests/ -v -x 2>&1 | tail -30`

---

### Task 7: 更新数据库 schema

**Files:**
- Modify: `strategies/hrs/strategy.py`（`_ensure_db_schema` 方法）

- [ ] **Step 1: 添加新字段到数据库表**

找到 `_ensure_db_schema` 方法，在已有表结构中新增列：

```sql
-- V2.4: LV-RM 字段
ALTER TABLE hrs.signal_log ADD COLUMN IF NOT EXISTS bb_position REAL;
ALTER TABLE hrs.signal_log ADD COLUMN IF NOT EXISTS rsi_value REAL;
```

- [ ] **Step 2: 运行测试**

Run: `cd /Users/yl/vscode/Binance_quantitative_trading && python -m pytest strategies/hrs/tests/ -v -x 2>&1 | tail -30`

---

## 自检

### Spec coverage（对照文档逐项检查）

| 文档章节 | 实现任务 | 状态 |
|---------|---------|------|
| 1. 策略概述（三轨并行） | Task 4 | Task 4 实现 |
| 2.2 步骤4：LV-RM扫描范围 | Task 3 | Task 3 实现 |
| 3.5 LV-RM入场条件 | Task 2 | Task 2 实现 |
| 3.5 LV技术评分 | Task 2 | Task 2 实现 |
| 3.5 LV-RM总分计算 | Task 2 | Task 2 实现 |
| 4. 进场逻辑（决策流） | Task 4 | Task 4 实现 |
| 5. 止损止盈（独立配置） | Task 5 | Task 5 实现 |
| 6. 仓位管理（独立开仓限制） | Task 4 (daily_limit) | 需在 strategy.py 中实现LV-RM独立计数 |
| 7. 数据库字段 | Task 7 | Task 7 实现 |
| 8. 版本记录 | Task 1 | Task 1 实现 |